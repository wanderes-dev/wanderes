# Project State (resume point)

> Purpose: let Claude (in any future session) resume exactly where the last session left off, without re-reading the entire conversation history. Update this file every time meaningful progress is made or the project pauses. This is the single source of truth for "where did we stop."

**Last updated:** 2026-09-02 (country entry-requirements table added)
**Current phase:** Phases 0-17 complete. **Phase 18 (MVP Validation): the app is deployed and live** at https://wanderes.com/ (the old https://travelagent-web.onrender.com/ URL also still resolves to the same service - see the 2026-09-02 infra-rename entry below for why). The first deploy attempt turned out not to go through the Blueprint flow at all (a single service was created manually, so `render.yaml`'s wiring never applied to it) - once redone via "New > Blueprint" properly, six real bugs surfaced and were fixed live: a bare `/` 404, a silently-swallowed DB connection error, the actual missing `DATABASE_URL` (resolved by using the Blueprint flow), a Celery worker crash on `DJANGO_ALLOWED_HOSTS`, a worker OOM from unbounded concurrency, and a curated-destinations data file excluded from the Docker image by `.dockerignore`. Full account in `DEVELOPMENT_LOG.md`'s "First real Render deploy" entry. Real-user validation (talking to users, watching behavior - the actual substance of Phase 18) has not started yet; that's the user's work, not Claude Code's. The whole site now has real CSS/JS (previously only `/chat/` did), and the chat pipeline has real Redis-backed conversation memory plus several live-reported bugs fixed along the way (an anonymous-user login-wall dead-end, a clarification loop on an optional field, a static-files manifest gap, and - most recently, 2026-08-31 - the AI still jumping to destination suggestions with too little information, a future_intent misclassification, and the chat input bar not staying visually pinned). All of this is detailed in the dated checklist entries below and in `DEVELOPMENT_LOG.md` - this summary line intentionally stays short; that's where the full history and reasoning live. 169/169 tests passing, `ruff check .` clean as of the last entry.

Also live: an out-of-sequence enhancement where the chat itself can capture feedback, register a past visit, or register a future travel intention conversationally (a working, live-validated chat UI at `/chat/` — start it with `docker compose up -d`, open http://localhost:8000/chat/, see README); feedback (chat or standalone form) genuinely influences future recommendations via a background Celery task (Phase 15); self-hosted, first-party product analytics (Phase 17, see `DECISIONS_PENDING.md` §3). **Important tooling note**: run local Dockerized tests as `docker compose exec -e DJANGO_SETTINGS_MODULE=config.settings.test web pytest`, not without the `-e` override - see `CLAUDE.md` for why. Repo is pushed to https://github.com/wanderes-dev/wanderes.git (remote `origin`, branch `master`).

**Product decision (2026-08-29):** UI is English-only for now, built translation-ready (`LocaleMiddleware`, `LOCALE_PATHS`, `LANGUAGES` already configured — see `CLAUDE.md` rule 5). Working/communication language with the user also switched to English as of this date.

## ✅ RESOLVED — Docker virtualization blocker (2026-08-28)

The user enabled hardware virtualization (BIOS/UEFI) and the "Virtual Machine Platform" Windows feature, then rebooted, as instructed in the previous session. Confirmed working: `docker info` now returns a healthy WSL2-based engine (`Kernel Version: 6.6.114.1-microsoft-standard-WSL2`, Hyper-V requirements satisfied).

**Full Docker Compose validation of Milestone 1 was then run end-to-end and passed:**

1. Created local `.env` from `.env.example` (generated a random `DJANGO_SECRET_KEY`; `.env` is gitignored, not committed).
2. `docker compose up --build -d` — built `web` and `worker` images, started `db` (Postgres 16, healthy), `redis` (7, healthy), `web`, `worker` — all containers came up clean.
3. `docker compose exec web python manage.py migrate` — all built-in Django migrations (admin, auth, contenttypes, sessions) applied without error.
4. `docker compose exec web pytest` — 1 passed (`core/tests/test_health.py`).
5. `curl http://localhost:8000/health/` from the host → `{"status": "ok", "database": "ok"}`, HTTP 200.
6. Celery worker logs confirmed clean startup: connected to `redis://redis:6379/0`, registered `config.celery.debug_task`, `celery@... ready.`
7. Stack was brought down afterward with `docker compose down` (validation run only, nothing needs to stay running).

**Milestone 1's Definition of Done (`14_MVP_IMPLEMENTATION_PLAN.md` §4) is now fully complete.** The project is no longer paused for infrastructure reasons — the remaining blocker is purely the Phase 2/3 human decisions below.

## Where we are

Per `15_IMPLEMENTATION_GUIDE.md`, completed phases:

- [x] Phase 0 — Prepare development environment
- [x] Phase 1 — Project foundation (Django + PostgreSQL + Redis + Docker Compose + Celery skeleton + CI + health check + first test) — **fully validated in Docker, 2026-08-28**

Blocked / not started:

- [x] **Phase 2 — Select AI provider** ✅ Decided 2026-08-28: **OpenAI**, behind the internal AI Provider Abstraction. Assistant persona named **"Wander"** (originally "Lunna", renamed 2026-08-30 per direct user request). See `DECISIONS_PENDING.md` §1 for the full decision record and implementation priorities (conversation context vs. persistent memory, context summarization, relevant-data-only, caching). **Adapter not yet implemented — next actionable step.**
- [x] **Phase 3 — Select travel data providers** ✅ Decided 2026-08-28: **curated static dataset** for destination data (name/country/description/POIs) + **Open-Meteo** for real climate data, both behind the internal Travel Data Interface. Flights/hotels deferred per the MVP plan. See `DECISIONS_PENDING.md` §2. Initial curated destination list (18 destinations) drafted and approved 2026-08-29 — stored at `travel/data/curated_destinations.json` (moved from `documentation/data/` 2026-08-30 - see the Phase 18 deploy entry below for why). **Adapters (Open-Meteo client + destination data access) not yet implemented — next actionable step.**
- [x] **Phase 4 — Define initial domain models (Joint Review)** ✅ Done 2026-08-29. Proposed, reviewed/iterated, and implemented. See `DEVELOPMENT_LOG.md` for the full design discussion. Summary:
  - `users.User` — custom user model, **email-based login** (no username field). Google OAuth is a planned future login method (per user request) but **not implemented yet** — deferred, same as `14_MVP_IMPLEMENTATION_PLAN.md` Milestone 2 originally specified.
  - `users.TravelerProfile` — deliberately minimal (preferred trip types, preferred cost-of-living tier).
  - `travel.Destination` — shaped to match `travel/data/curated_destinations.json` exactly; loadable via `python manage.py load_destinations`.
  - `trips.Trip`, `trips.TripFlight`, `trips.TripAccommodation`, `trips.Feedback` — `TripFlight`/`TripAccommodation` are real relational models (not JSON), so prices/ratings stay properly typed and a trip can have multiple flight legs (connections) as ordered rows. `Feedback` has a DB-level constraint requiring at least a destination or a trip.
  - `TripItem` (generic) and community-intelligence entities (`CommunityReview`, etc.) explicitly deferred to later milestones/phases, per `04_DATABASE_DESIGN.md`'s "avoid speculative entities."
  - Migrations validated end-to-end in Docker (`makemigrations` → `migrate` → `pytest` — 9 passing model tests — → `ruff check .` clean). The curated dataset was loaded for real (18/18 destinations).
  - **Note:** validating this required dropping and recreating the local dev Postgres volume (`docker compose down -v`), since `AUTH_USER_MODEL` can't change after migrations have been applied against it. The old volume only held the empty Milestone 1 smoke-test state — no real data was lost.
- [x] **Phase 5 — Authentication** ✅ Done 2026-08-29. Django's built-in auth system (session-based, per `07_API_DESIGN.md` §3 — no JWT/separate auth API), adapted for email login:
  - `users:register` — custom `UserRegistrationForm` (email + password), auto-logs in on success.
  - `users:login` / `users:logout` — Django's built-in `LoginView`/`LogoutView`, working out of the box with the custom email `USERNAME_FIELD` (no custom form code needed).
  - `users:account` — minimal `@login_required` page (Milestone 2 DoD's "authenticated area"), shows email + join date.
  - `LOGIN_URL`/`LOGIN_REDIRECT_URL`/`LOGOUT_REDIRECT_URL` configured; unauthenticated access to `account` redirects to login with `?next=`.
  - Google OAuth remains explicitly deferred (not implemented) — the user only asked for the door to stay open for it later.
  - 7 new tests (registration, login success/failure, logout, login-required redirect) — 16/16 total passing. Verified live in a real browser (register → auto-login → account → logout → login → account) in addition to automated tests. `ruff check .` clean, no new migrations needed.
- [x] **Phase 7 — First travel data integration** ✅ Done 2026-08-29 (done ahead of Phase 6, per explicit user request). New `integrations` app (no models, pure Python + one Django app registration):
  - `integrations.climate.ClimateProvider` — internal Travel Data Interface (`10_EXTERNAL_INTEGRATIONS.md` §3), an ABC with `get_monthly_climate(latitude, longitude, month, year=None) -> MonthlyClimateSummary`.
  - `integrations.climate.open_meteo.OpenMeteoClimateProvider` — the Phase 3-decided adapter, calling Open-Meteo's free Historical Weather (archive) API. Validated, normalized (averages daily highs/lows/precipitation), timeout (5s), and provider errors wrapped in `ClimateProviderError` rather than leaking raw provider details.
  - `integrations.climate.get_climate_provider()` — factory reading `settings.CLIMATE_PROVIDER` (default `"open_meteo"`), so swapping providers is a settings change, not an app-code change.
  - Redis-backed caching (7-day TTL — historical data for a past month doesn't change) per `10_EXTERNAL_INTEGRATIONS.md` §7.
  - **Documented MVP simplification:** without an explicit `year`, the adapter uses the most recently completed occurrence of the requested month as a stand-in for "typical" conditions — not a genuine multi-year climatological average. Noted as a natural future improvement, not a current requirement.
  - 7 new tests (mocked HTTP — success/cache-hit/network-failure/malformed-response/factory), all passing. **Also smoke-tested against the real Open-Meteo API** (not mocked): Lisbon in October 2025 → 24.8°C avg high, 17.1°C avg low, 48.6mm precipitation — consistent with the curated dataset's "best season Mar-Oct" for Lisbon.
  - Added `requests` to `requirements/base.txt`.
- [x] **Phase 6 — Traveler profile (edit/retrieve, authorization)** ✅ Done 2026-08-29 (out of order, after Phase 7, per user request). Reuses the `TravelerProfile` model from Phase 4 — kept intentionally minimal per `14_MVP_IMPLEMENTATION_PLAN.md` Milestone 6 ("do not create a large questionnaire unnecessarily... grow organically").
  - `users:profile` — `@login_required` view, always operates on `request.user`'s own profile (never accepts a profile id from the URL, so cross-user access isn't possible by construction), `get_or_create`s the profile on first visit.
  - `TravelerProfileForm` — `preferred_trip_types` rendered as checkboxes (`CheckboxSelectMultiple`) instead of a raw JSON textarea; `preferred_cost_of_living` as a normal select.
  - Linked from the account page ("Edit my traveler profile").
  - **Note on Milestone 6's full DoD** ("Wanderes can use authorized profile information to personalize recommendations"): only the edit/retrieve/authorization half is done here — the recommendation engine that would consume this data doesn't exist yet (Phase 8). Expected at this point in the sequence, not a gap specific to this phase.
  - 4 new tests (login-required redirect, auto-create on first visit, save persists correctly, one user's edits don't affect another's) — 27/27 total passing. `ruff check .` clean, no new migrations. Verified live in a browser: checked "Beach" + "Culture", selected "Medium" cost tier, saved, confirmed success message and correct DB persistence.
- [x] **OpenAI adapter (Phase 2's remaining deliverable / Milestone 4 — AI Foundation)** ✅ Done 2026-08-29. New `ai` app (no models, same style as `integrations`):
  - `ai.provider.AIProvider` — internal AI Provider Abstraction (`05_AI_DESIGN.md` §10, `09_AI_ORCHESTRATION.md` §11), ABC with `generate_reply(messages, max_tokens=None) -> AIResponse`. `AIMessage`/`AIResponse` are plain dataclasses so the interface stays provider-agnostic.
  - `ai.provider.openai_provider.OpenAIProvider` — calls OpenAI's Chat Completions API. Validates/normalizes the response, wraps `OpenAIError`/empty-content/malformed-response cases in `AIProviderError`, defaults `max_tokens=600` for cost containment, raises a friendly `ImproperlyConfigured` (pointing at `.env.example`) if `OPENAI_API_KEY` is blank rather than a cryptic SDK error.
  - `ai.provider.get_ai_provider()` — factory reading `settings.AI_PROVIDER` (default `"openai"`), same swappable-via-settings pattern as the climate provider.
  - `ai/prompts.py` — the `SYSTEM_PROMPT` constant naming the assistant "Wander" (per the Phase 2 decision, renamed from "Lunna" 2026-08-30) and encoding two product rules: travel-only scope (`09_AI_ORCHESTRATION.md` §10) and never inventing travel data (`05_AI_DESIGN.md` §7). Not auto-injected by the adapter — callers (the future orchestration layer, Phase 9) prepend it explicitly.
  - **Explicitly not built here:** the actual orchestrator that constructs context from `TravelerProfile`/`Destination`/conversation history, summarization, and response caching — those are Phase 9 (AI orchestration) concerns. This phase only delivers the swappable provider adapter itself, matching Milestone 4's scope.
  - Added `openai` to `requirements/base.txt`; `OPENAI_API_KEY`/`AI_MODEL` documented in `.env.example` (key left blank — real value is the user's to provide, never committed).
  - 6 new tests, all mocking the OpenAI client (no real API calls, unlike the free Open-Meteo adapter — a real call costs money on the user's account and needs a real key we don't have). 33/33 total tests passing, `ruff check .` clean, no new migrations. Manually confirmed the missing-key path raises the intended friendly error.
- [x] **Phase 8 — First recommendation algorithm** ✅ Done 2026-08-29. New `recommendations` app (no models — pure Python, mirroring `integrations`/`ai`), scoped strictly to `05_AI_DESIGN.md` §2/§5/§6 and Milestone 5's "Recommendation Logic" (Candidate Destination → Hard Constraints → Basic Score → Ranking). **Natural-language intent extraction and AI explanation are explicitly NOT part of this** — that's Phase 9.
  - `recommendations.scoring.RecommendationRequest` — already-extracted, deterministic constraints (`month`, `min_temp_c`/`max_temp_c`, `max_cost_of_living`, `excluded_slugs`, optional `user`).
  - `recommendations.scoring.generate_recommendations()` — queries `Destination`, fetches live climate via `integrations.climate.get_climate_provider()` (injectable for tests), applies hard constraints (temperature range, cost ceiling, exclusions), then scores survivors on: preference fit (matches `TravelerProfile.preferred_trip_types`), budget fit (headroom under the cost ceiling), temperature fit (margin above the minimum), and a repetition penalty (soft, not a hard exclusion) for destinations with a `completed` `Trip` — per `05_AI_DESIGN.md` §5's "previously visited... lower priority, not excluded". A destination the climate provider can't return data for is skipped gracefully (`10_EXTERNAL_INTEGRATIONS.md` §5), not a hard failure.
  - Community/feedback-based scoring terms from `05_AI_DESIGN.md` §6's full formula are **deliberately not included yet** — those belong to their own later milestones (Community Intelligence, Feedback & Learning) requiring aggregation/privacy safeguards this phase doesn't build.
  - 8 new tests, all with an injected stub climate provider (no network calls) — hard constraints, exclusions, ranking order, preference/repetition scoring, graceful degradation, and anonymous-user behavior. 41/41 total tests passing, `ruff check .` clean, no new migrations.
  - **Also validated against real, live data**: loaded the 18 curated destinations and ran `generate_recommendations(RecommendationRequest(month=10, min_temp_c=20.0, max_cost_of_living=3))` with the real Open-Meteo provider — returned a plausible, correctly-ranked list (Marrakech and Chiang Mai topped it: cheapest and genuinely warmest in real October climate data).
- [x] **Phase 9 — AI orchestration** ✅ Done 2026-08-29. `ai.orchestration.get_travel_recommendation(message, user=None)` - a stateless, single-message orchestrator per `09_AI_ORCHESTRATION.md` §3's pipeline (Intent Understanding → Travel Data/Rules → Recommendation Scoring → AI Reasoning). **Explicitly not built**: conversation history/persistence and streaming - both need the chat interface (Phase 10) to exist first.
  - Extended `AIProvider` with `generate_structured_reply(messages, json_schema)` - a generic structured-output method any provider adapter must support, not an OpenAI-only escape hatch, keeping the provider abstraction real. `OpenAIProvider` implements it via `response_format={"type": "json_schema", ...}`.
  - **Intent extraction**: first AI call, structured JSON output (`is_travel_request`, `needs_clarification`, `clarification_question`, `month`, `min_temp_c`, `max_cost_of_living`). The model is instructed to never guess a missing month — it asks a clarification question instead. Python-side validation double-checks the extracted values regardless of the schema (`09_AI_ORCHESTRATION.md` §9 — never fully trust model output).
  - **Scoring**: unchanged call into Phase 8's `recommendations.scoring.generate_recommendations()`.
  - **Explanation**: second AI call (plain text, not structured), prompted with only the top 5 already-ranked candidates and an explicit "do not invent any other destinations or facts" instruction — grounds the explanation in real, deterministic data.
  - Three short-circuit paths that skip the (paid) explanation call entirely: off-topic message, needs-clarification, and no-matches — each returns a canned/direct reply instead.
  - Any `AIProviderError` anywhere in the pipeline is caught and replaced with one generic fallback reply (deliberately simple — no partial-result salvage logic yet).
  - 8 new tests (6 orchestration + 2 for the new `generate_structured_reply` method), all using stub AI/climate providers — no network calls. 49/49 total tests passing, `ruff check .` clean, no new migrations.
  - **Update 2026-08-29 (same day):** the user provided a real `OPENAI_API_KEY`, now set in `.env` (never committed — `.env` is gitignored). A minimal live call confirmed the key works. A full live run of `get_travel_recommendation("I want somewhere warm in October, not too expensive")` surfaced a real bug: the "never guess a number" instruction was *too* conservative — qualitative words like "warm"/"not too expensive" came back with `min_temp_c`/`max_cost_of_living` as `null`, so the hard-constraint/scoring layer never actually engaged (all 18 destinations tied at score 0) and the second AI call ended up doing the real filtering itself from raw, unranked data — not the intended "AI reasons over already-scored candidates" design. **Fixed** by adding explicit interpretation anchors to `INTENT_EXTRACTION_SYSTEM_PROMPT` (e.g., "warm" → `min_temp_c=22`, "not too expensive" → `max_cost_of_living=3`) — this is the AI *interpreting the user's own words* into an operational threshold, not inventing a destination fact, so it doesn't violate the anti-hallucination rule. Re-ran live after the fix: correctly narrowed to 8 destinations with differentiated scores, top result (Marrakech/Chiang Mai) matching Phase 8's pure-scoring test exactly. This refinement is validated by live testing, not a unit test — prompt-interpretation quality isn't meaningfully mockable.
- [x] **Phase 10 — Chat interface** ✅ Done 2026-08-29. Django templates + a small amount of JS (no React, per the docs). This is Milestone 5's "First Vertical Slice" checkpoint — a person can now actually talk to Wander through a browser.
  - `GET /chat/` — the chat page (`ai/templates/ai/chat.html`), open to anonymous users (registered users get personalization for free since the view passes `request.user` through when authenticated).
  - `POST /api/v1/recommendations/` — matches the path already sketched in `07_API_DESIGN.md` §8. Returns a real `StreamingHttpResponse` (`text/plain`) of the reply text, built on `ai.orchestration.stream_travel_recommendation()` (extended in this phase from the Phase 9 pipeline — see below).
  - Client-side JS (`fetch` + `ReadableStream`, no framework): appends the user's message immediately, shows a "Wanderes is thinking..." bubble (wording changed same day per user request; originally "Lunna is thinking..."), replaces it with the incrementally-arriving reply, handles network failure and non-200 responses with a visible error bubble, and disables the input while a request is in flight (loading state).
  - Request Validation added at the view (empty or >2000-char messages rejected with 400 before any AI call is made) — the pipeline step named in `09_AI_ORCHESTRATION.md` §3 that hadn't been implemented anywhere yet.
  - **Explicitly deferred:** true multi-turn conversation memory. Each message the user sends is still processed independently by the orchestrator; the page only keeps a growing visual thread for the current browser visit, it doesn't feed earlier turns back into context. Real conversation memory is future work, not assumed to exist yet.
  - **Orchestrator extended for real streaming**: added `AIProvider.stream_reply()` to the interface (any provider must support it) and `OpenAIProvider`'s implementation via the SDK's `stream=True`. Rebuilt `ai.orchestration` around a new `stream_travel_recommendation()` as the core pipeline (mirrors the old `get_travel_recommendation()` exactly, but yields the explanation incrementally); `get_travel_recommendation()` is now a thin wrapper that joins the chunks, kept for tests/simple callers.
  - 12 new tests (3 for `stream_reply`, 3 for the streaming orchestrator path, 6 for the views) — 61/61 total passing, `ruff check .` clean, no new migrations.
  - **Real, live browser validation** (not just mocked tests): loaded `/chat/` with the real OpenAI key, sent "somewhere warm in October, not too expensive" — got the same Marrakech/Chiang Mai/Hoi An answer as the shell tests, streamed incrementally into the page. Also verified live: the off-topic scope guard ("what's the capital of France?"), the clarification path ("somewhere warm and cheap" with no month → Wander asked which month), and that both exchanges correctly accumulate in the on-page thread.
  - **Real bug found and fixed during this live testing**: `templates/base.html` was missing `<meta name="viewport">` entirely (true since Phase 1, not just this page). On a mobile-emulated viewport, `window.innerHeight` came back as 2123 instead of 812, so the chat page's `height: 100vh` container rendered nearly 3 screens tall with the input pushed far below the fold. Added the standard `width=device-width, initial-scale=1` viewport meta tag to `base.html` (fixes it site-wide, not just for chat) and reconfirmed `innerHeight` correctly reports 812 with the input properly pinned to the bottom of one screen.
- [~] **Phase 11 — First End-to-End MVP (Joint Review)** ⚠️ In progress 2026-08-29. Ran all 7 real scenarios named in `15_IMPLEMENTATION_GUIDE.md` (warm, budget, romantic, beach holiday, city break, family travel, strong exclusions) live against the real OpenAI API + real climate data. Findings:
  - ✅ **Warm / budget destination** — worked correctly out of the box.
  - ❌ **Found & fixed same day:** beach holiday, city break, and exclusions ("please not X") had **zero effect** on the deterministic `recommendations` list — `RecommendationRequest` had no `trip_type` field, and there was no path from natural language to `excluded_slugs` at all. Confirmed concretely: asking to exclude Chiang Mai still returned it ranked #2. Fixed:
    - `recommendations.scoring.RecommendationRequest` gained a `trip_type` hard constraint (matches `Destination.trip_type`).
    - `travel.services.find_destination_slugs_by_name()` (new) resolves free-text place/country names (case-insensitive, matches name or country) into slugs.
    - `ai.orchestration`'s intent schema gained `trip_type` (enum: beach/city/nature/culture/null — the model is explicitly told not to force-fit "romantic"/"family" into one of these) and `excluded_place_names` (array), both validated defensively like the existing fields.
    - Re-verified live: beach holiday → 3/18 correctly beach-only; city break → 5/18 correctly city-only; Chiang Mai exclusion → genuinely absent from the data now, not just coincidentally omitted from the AI's text.
    - 10 new tests (trip_type hard constraint, `find_destination_slugs_by_name` unit tests, orchestrator-level trip_type/exclusion tests) — 71/71 total passing, `ruff check .` clean, no new migrations.
  - ✅ **Recommendation philosophy decided 2026-08-29 (human decision):** for requests with no deterministic model (e.g. "romantic", "family-friendly"), the AI answers from its own general knowledge rather than the app trying to predict every possible category in advance — this is intentional, not a gap to keep closing. What the app does do is **log** these cases (and genuine failures) for future review, so real usage — not guesswork — informs which dimensions eventually get formalized. Implemented as structured logging in `ai.orchestration` (Python `logging`, not a new DB model — that's premature before a real analytics need, per `15_IMPLEMENTATION_GUIDE.md`'s general philosophy):
    - INFO when a valid travel request extracts no deterministic constraints at all (trip_type/min_temp_c/max_cost_of_living all null) — "relying on AI judgment."
    - INFO when hard constraints eliminate every candidate (no matches).
    - WARNING when an `AIProviderError` occurs (intent extraction or mid-stream) — a genuine failure, not a philosophy choice.
    - 3 new tests using Django's `assertLogs`; 74/74 total passing, `ruff check .` clean. Confirmed live: a real "romantic getaway" request logged exactly the intended line.
  - Still open, not addressed by this decision: in a couple of replies during testing the AI added supplementary facts not present in our dataset (e.g. "Lisbon has nearby beaches," "Kyoto is known for safety") — a grounding concern, separate from the recommendation-philosophy question.
  - **✅ Human Decision made 2026-08-29: "yes, continue expanding."** Phase 11 is closed.
- [x] **Phase 12 — Travel History** ✅ Done 2026-08-29. Per `04_DATABASE_DESIGN.md` §2/§4, Travel History is a distinct entity from `Trip` (a much simpler standalone record — "visited X, roughly year Y" — not a full itinerary with items). Note: the existing `Trip.status="completed"` repetition-penalty logic from Phase 8 was real code but practically unreachable by any user until now, since there's still no Trip-creation UI (that's Phase 13).
  - `trips.TravelHistoryEntry` (new model): `user`, `destination`, `visited_year` (nullable — "approximate... where useful").
  - Full CRUD at `/trips/history/` (`login_required`; structural authorization — every view scopes to `user=request.user`, confirmed via tests that another user's entries 404 rather than leak or allow editing).
  - `recommendations.scoring._visited_destination_slugs()` now unions completed `Trip`s **and** `TravelHistoryEntry` records — either counts as "visited" for the soft repetition penalty (not a hard exclusion, per the "important rule" in both `05_AI_DESIGN.md` §5 and this phase's own framing).
  - Linked from the account page ("My travel history").
  - 10 new tests (model, 7 view/authorization cases, repetition-penalty-from-history) — 83/83 total passing, `ruff check .` clean. One new migration (`trips.0003_travelhistoryentry`).
  - **Verified live, including the actual scoring effect**: added "Chiang Mai, 2019" via the real browser UI for a real logged-in user, then confirmed via `get_travel_recommendation(..., user=that_user)` against the real OpenAI + Open-Meteo APIs that Chiang Mai's `repetition_penalty` became `3.0` and it dropped from top-ranked to 5th place — present, just deprioritized, exactly as intended.
- [x] **Phase 13 — Trip Management** ✅ Done 2026-08-29. Per `15_IMPLEMENTATION_GUIDE.md` Phase 13 / `14_MVP_IMPLEMENTATION_PLAN.md` Milestone 8: create/edit/view/delete a `Trip` (name, destination, dates), plus "save relevant recommendations." **Do not build advanced itinerary management** — respected; no new Trip Item types added.
  - `Trip` gained a `name` field (nickname, e.g. "Summer in Lisbon"; blank falls back to `"{destination}"` in `__str__`/display). Migration `trips.0004_trip_name`.
  - Full CRUD at `/trips/` (list), `/trips/create/`, `/trips/<pk>/` (detail), `/trips/<pk>/edit/`, `/trips/<pk>/delete/` — all `login_required`, structurally scoped to `user=request.user` (another user's trip 404s, matching the established pattern).
  - **"Save relevant recommendations" implemented for real**, not skipped as a stretch goal: `/api/v1/recommendations/`'s streaming response now appends a trailing delimiter + JSON footer (`[{slug, name, country}, ...]` for the top recommended destinations) after the visible reply text. The chat page's JS buffers the stream, strips the footer out of the displayed bubble text, parses it, and renders a "Save as trip" link per destination pointing at `/trips/create/?destination=<slug>` (which pre-fills that destination in the create form).
  - Linked from the account page ("My trips").
  - 16 new tests (Trip CRUD + authorization, prefill-from-query-param, and the recommendations-footer behavior on the streaming view) — 93/93 total passing, `ruff check .` clean.
  - **Verified the full real, live flow end-to-end**: asked Wander for a real recommendation in the browser → got 5 "Save as trip" links with clean (non-leaked) reply text → clicked "Save Marrakech as a trip" → form opened with Marrakech pre-selected → saved as "October trip" → landed on the trip detail page → confirmed it appears in the trip list.
- [x] **Phase 14 — Feedback** ✅ Done 2026-08-29. Per `15_IMPLEMENTATION_GUIDE.md` Phase 14 / `14_MVP_IMPLEMENTATION_PLAN.md` Milestone 9 (the Feedback half only — "Learning From Feedback," i.e. using it to influence recommendations, is Phase 15, a separate Joint Review phase not yet done). Explicitly flagged as a **Human Decision** in the guide ("Define the initial feedback taxonomy. Keep it small.") — proposed and got approval before implementing:
  - **Approved tag taxonomy** (`trips.FEEDBACK_TAG_CHOICES`, 8 tags): Excellent food, Great value, Friendly locals, Beautiful scenery / Too crowded, Overpriced, Poor weather, Hard to get around. Not enforced as a DB `choices=` constraint (`Feedback.tags` is a JSONField list) — `FeedbackForm` restricts the UI to these values via `CheckboxSelectMultiple`.
  - `Feedback` model unchanged (already existed from Phase 4 with `rating` 1–10, `tags`, `comment`, `destination`/`trip`) — this phase adds the actual form/view/persistence/tests, which didn't exist yet.
  - `/trips/<pk>/feedback/`: one feedback entry per (user, trip) — resubmitting **edits** the existing entry rather than creating a duplicate (`get_or_create`-style lookup, not a hard DB unique constraint). Structurally scoped to `user=request.user` like every other trip view.
  - Trip detail page now shows existing feedback (rating, human-readable tag labels — not raw keys like `excellent_food` — and comment) with an edit link, or a "Leave feedback" link if none exists yet.
  - 6 new tests (create, reject out-of-range rating, resubmission updates not duplicates, cross-user 404, tag-label display) — 99/99 total passing, `ruff check .` clean, no new migrations.
  - **Verified live**: opened the feedback form for the trip saved in Phase 13, checked "Excellent food" + "Too crowded," rated 8/10, added a comment, saved — trip detail page correctly showed "Rating: 8/10," "Tags: Excellent food, Too crowded" (proper labels, not keys), and the comment.
- [x] **Conversational feedback/history/future-intent capture** ✅ Done 2026-08-29 — an out-of-sequence enhancement the user requested directly (not a numbered guide phase): "no need to go to another page" to give feedback, and the AI should register a past visit or a stated future travel intention straight from the chat. Extends the delivery channel for Phases 12-14's existing models (`TravelHistoryEntry`, `Trip`, `Feedback`) - does **not** change what Phase 15 ("Learning From Feedback") still needs to do, since none of this yet influences recommendation scoring.
  - `ai.orchestration`'s single intent-extraction call now classifies every message into `message_type`: `recommendation` (existing flow, unchanged), `feedback`, `future_intent`, or `off_topic` - one AI call, not a separate classification round-trip, to avoid tripling the cost of the most common (recommendation) path.
  - **Feedback via chat**: extracts destination name, an optional 1-10 rating (never invented from a neutral statement), tags (from the same approved 8-tag taxonomy as the standalone form), and a comment. Giving feedback always registers `TravelHistoryEntry` too (visiting is implied by having an opinion about it) - directly matching the user's "register a travel that occurred" requirement. If no rating was given, only the history entry is registered (no incomplete `Feedback` row, since `rating` is a required field). Resubmitting updates the same `(user, destination, trip=None)` record rather than duplicating - independent from any trip-specific feedback given via `/trips/<pk>/feedback/`.
  - **Future travel intent via chat**: extracts a destination name and creates a `Trip` with `status="planned"` (`get_or_create`, so repeating the same intent doesn't duplicate).
  - Anonymous users get a clear "log in to save this" reply for feedback/future-intent (there's no account to attach the record to) - recommendations still work anonymously as before.
  - An unrecognized destination name (not in the curated dataset) degrades gracefully - acknowledges the message without crashing or persisting a broken reference, and logs it for future catalog-gap review (same "log what we couldn't act on" pattern from Phase 11).
  - Acknowledgment replies for feedback/future-intent are templated (not a second AI call) - keeps cost down for what's fundamentally a confirmation, consistent with the project's cost-conscious defaults.
  - 8 new tests (feedback with/without rating, resubmission updates not duplicates, cross-type login gating, future-intent trip creation, unrecognized-destination graceful handling) - 106/106 total passing, `ruff check .` clean, no new migrations (reuses all three existing models unchanged).
  - **Verified live in one real conversation**, all three non-recommendation paths plus a regression check on the original recommendation flow: "I went to Marrakech... 9/10" → real `Feedback` + `TravelHistoryEntry` created; "I want to visit Bali someday" → real `Trip` (planned) created; "somewhere warm and cheap in December" → still works exactly as before, including the Phase 13 "Save as trip" links.
- [x] **Phase 15 — Learning From Feedback** ✅ Done 2026-08-29 (Joint Review). Per the guide's explicit **Human Decision** ("define what information may automatically change a user's profile — not every piece of feedback should permanently change preferences"), proposed and got approval for a specific, conservative rule set before implementing:
  - **Background Celery task** (`trips.tasks.update_traveler_preferences_from_feedback`) triggered by a `post_save` signal on `Feedback` (`trips/signals.py`, connected in `TripsConfig.ready()`) — matches the flow diagram (`Feedback → Persist → Background Processing → Update Preferences`) and means the user never waits for it.
  - **Recomputes from all of a user's feedback every run** (not incremental counters) — this is what makes it retry-safe and duplicate-processing-safe (explicit review criteria): running it twice, or out of order, produces the same result.
  - **`preferred_trip_types`**: additive only, added after ≥2 ratings of 8+ for that trip type. Never removes a type (whether user-set or previously learned) — accepted, documented edge case: manually removing a type doesn't "stick" against continued strong positive feedback.
  - **`preferred_cost_of_living`**: only auto-set if the user hasn't already set it manually — an explicit choice always wins over an inferred one. Also requires ≥2 ratings of 8+ for that cost tier.
  - This is this project's **first real Celery task** — infrastructure existed since Milestone 1 with zero tasks, per the "don't create background jobs until a specific need justifies them" principle; this is that need.
  - **Real infrastructure bug found and fixed along the way**: `docker compose exec web pytest` had been silently running the entire test suite under `config.settings.development` instead of `config.settings.test` since Milestone 1 — `docker-compose.yml` hardcodes `DJANGO_SETTINGS_MODULE=config.settings.development` as an environment variable for the `web`/`worker` services, and pytest-django gives an existing env var priority over `pyproject.toml`'s ini-option setting. This was harmless until now (no earlier test depended on a setting that actually differs between the two files - e.g. `CELERY_TASK_ALWAYS_EAGER`), but this phase's signal-triggered task made the gap concretely visible: the task silently never fired in "eager" tests because eager mode wasn't actually active. **CI was never affected** — `.github/workflows/ci.yml` already sets `DJANGO_SETTINGS_MODULE=config.settings.test` explicitly as a job-level env var. **Fix**: run local Dockerized tests as `docker compose exec -e DJANGO_SETTINGS_MODULE=config.settings.test web pytest` — see the new note in `CLAUDE.md`.
  - 9 new tests (additive trip_type logic, low-rating non-addition, never-removes-existing, cost-of-living set-when-unset / not-overridden, idempotent recompute, no-feedback no-op, and the signal actually firing) — 115/115 total passing (once run under the correct settings), `ruff check .` clean, no new migrations.
  - **Verified live against the real Celery worker** (not eager mode): created real feedback via shell, confirmed in `docker compose logs worker` that the task was received and processed by the actual worker process, then confirmed the real `TravelerProfile` was updated correctly — `preferred_trip_types` gained `beach` after two 9/10 ratings, while `preferred_cost_of_living` correctly stayed at its existing manually-set value (`3`, from Phase 6 testing) rather than being overridden by the new signal.
- [x] **Phase 16 — Introduce Background Processing Where Needed** ✅ Reviewed and closed 2026-08-30 (Joint Review) — **no new background jobs added**, by design. The guide's own instruction for this phase is "only now should we identify which operations actually benefit from background processing... do not move ordinary request/response work into background jobs simply because Redis exists" — reviewed each example the guide names against the current codebase and found none justified a new job:
  - **Feedback processing** — already backgrounded in Phase 15; nothing left to do.
  - **Community aggregation** — `CommunityReview`/`AggregatedInsight` don't exist yet (deliberately deferred since Phase 4); nothing to background because the feature itself doesn't exist.
  - **Non-critical notifications** — no notification system (email/push) exists anywhere in the project; would mean inventing a feature, not backgrounding an existing one.
  - **Provider synchronization** — the one live candidate considered: a scheduled Celery Beat task pre-warming the Redis climate cache for all 18 curated destinations, instead of `recommendations.scoring.generate_recommendations()`'s current per-request synchronous fetch. Presented to the user as an option (given the post-Phase-15 revision's note that this loop calls the climate provider once per candidate); **declined** — at 18 destinations, a free/fast API, and an existing 7-day cache, the benefit is marginal (only smooths the first cache-miss request after each expiry), so adding scheduled infrastructure for it now would itself be exactly the "background job simply because Redis exists" anti-pattern the phase warns against.
  - No code changes, no new tests, no migrations — this phase's deliverable is the review record itself.
- [x] **Phase 17 — Product Analytics** ✅ Done 2026-08-30 (Human Decision + Claude Code). Full decision record in `DECISIONS_PENDING.md` §3; summary:
  - New `analytics` app: `Event` model (`event_type`, nullable `user` FK with `on_delete=SET_NULL`, nullable `anonymized_ip`, a small structured `metadata` JSONField, `created_at`), `analytics.services.record_event()` as the single write path, `analytics.metrics` for query-side metrics, registered read-only in Django admin.
  - **Events wired in now**: `user_registered` (`users:register`), `profile_completed` (`users:profile`, fires once — the first time the profile has real content, not on every edit), `travel_question_submitted` (`ai:recommendations-api`, fires for *any* chat message regardless of `message_type`), `recommendation_generated` (same view, when the pipeline actually returns ≥1 result), `trip_created` (`trips:trip-create` form and the conversational future-intent path, tagged with `metadata.source`), `feedback_submitted` (`trips:trip-feedback` form and the conversational feedback path, same `source` tagging, only when a rating was actually given).
  - **Deliberately not built**: `premium_started`/`affiliate_link_clicked` (features don't exist yet) and `recommendation_generated`'s sibling `recommendation_viewed` (redundant — recommendations already appear inline in the chat stream, no separate results page).
  - **Anonymous visitors tracked by anonymized IP** (last IPv4 octet zeroed / IPv6 masked to a /48 prefix in `analytics.services._anonymize_ip`, before the value ever reaches the database) rather than a session identifier — per the user's explicit instruction. Authenticated events store the user, never an IP.
  - `record_event()` never raises — a failure is logged and swallowed, so a non-critical analytics write can never break registration, chat, trip creation, or feedback (consistent with Phase 16's "don't let non-critical work affect the main request/response flow").
  - **A small, non-blocking transparency note** was added to `templates/base.html`'s footer (wrapped in `{% blocktrans %}`, i18n-ready per rule 5) disclosing anonymized usage-data collection — not a full cookie-consent banner, which the guide places in a later pre-launch GDPR review phase that hasn't started.
  - 34 new tests (IP anonymization, `record_event` behavior including the swallowed-failure case, all six metric functions, and wiring assertions at every call site) — 149/149 total passing, `ruff check .` clean, one new migration (`analytics.0001_initial`).
  - **A real design flaw was caught and fixed during implementation, before it shipped**: the `Event` model's first draft had a `CheckConstraint` requiring `user` or `anonymized_ip` to be non-null. Since `user` uses `on_delete=SET_NULL` (deliberately, so deleting an account doesn't retroactively distort historical aggregate metrics), that constraint would have made Postgres raise `IntegrityError` on the exact SET_NULL update triggered by deleting any user with analytics history — silently blocking the account-deletion capability `11_SECURITY_&_PRIVACY.md` requires. Removed the DB constraint; the "user or IP" invariant is enforced by `record_event()` at write time instead, and a regression test (`test_deleting_user_keeps_event_with_null_user`) covers the SET_NULL path directly.
  - **Verified live against the real running app**: confirmed the footer note renders on a real page; sent a real anonymous message through `/chat/` to the real OpenAI API — an off-topic message recorded `travel_question_submitted` with `anonymized_ip` correctly zeroed to a `/24` network address and no `recommendation_generated`; a real recommendation request ("somewhere warm in October, not too expensive") recorded both events, matching the same Marrakech/Chiang Mai result already validated in Phase 9-11.
- [~] **Phase 18 — MVP Validation** ⚠️ In progress 2026-08-30 — **Owner: Human** per the guide (put the app in front of real users, 10 → 100 → 1,000; talk to them, watch behavior, decide what's next). No real users exist yet, so the phase itself hasn't started. What's done so far is the practical prerequisite: the app can't reach real users while it only runs on `localhost`, so deploy-readiness work was prepared (user chose **Render** after being presented Render/Railway/Fly.io tradeoffs — hosting provider is explicitly a "decide later, based on cost/reliability/simplicity" call per `12_DEVELOPMENT_&_DEPLOYMENT.md` §14, not something to pick unilaterally):
  - **`Dockerfile`** gained a new, final `production` stage (multi-stage build) — installs `requirements/production.txt` only (no pytest/ruff), runs `collectstatic`, serves via `gunicorn` bound to `$PORT` (shell-form `CMD` so the platform-injected port actually expands) instead of `runserver`. It's deliberately the *last* stage so a plain `docker build .` (what Render runs with no `--target` flag) builds it by default. The existing `runtime` stage (dev/test image) is untouched; `docker-compose.yml` now pins `target: runtime` explicitly on both `web`/`worker` so local dev behavior is unchanged — reconfirmed via a full rebuild + 149/149 tests + `ruff check .` clean.
  - **`whitenoise`** added (`requirements/base.txt`, middleware + `STORAGES` in `config/settings/base.py`) so static files (mainly Django admin's CSS/JS) serve correctly under `gunicorn` without a separate CDN/nginx — simplest option per `12_DEVELOPMENT_&_DEPLOYMENT.md` §15.
  - **`config/settings/production.py`** gained two real, load-bearing additions beyond the post-Phase-15-revision fail-fast checks: (1) a fallback that appends Render's auto-set `RENDER_EXTERNAL_HOSTNAME` to `ALLOWED_HOSTS`, so the very first deploy works before a custom domain or manual `DJANGO_ALLOWED_HOSTS` override exists; (2) `SECURE_PROXY_SSL_HEADER` — without it, `SECURE_SSL_REDIRECT=True` (already set) would have caused an infinite redirect loop in production, since Render (like most PaaS platforms) terminates TLS at a proxy in front of the app and gunicorn only ever sees plain HTTP. Also added `CSRF_TRUSTED_ORIGINS` derived from `ALLOWED_HOSTS`.
  - **`render.yaml`** (new): a Render Blueprint declaring `travelagent-web` (Docker, the `production` stage, health-checked at `/health/`), `travelagent-worker` (same image, `dockerCommand` overridden to the Celery worker), a managed Postgres database, and a managed Key Value (Redis) instance — the same four services `docker-compose.yml` runs locally. Written against Render's current documented Blueprint spec (verified via live doc fetches during implementation, since Render has renamed/changed Blueprint fields over time - e.g. Redis is now `type: keyvalue`, not `type: redis`) but **not yet validated against an actual Render deploy** - this repo has no Render account, and creating one / entering payment details is a human action, not something Claude Code does. One documented Blueprint gap handled explicitly: Render's spec has no way to share one service's plain env var with another (only connection-property references like `fromDatabase`/`fromService` for `connectionString`), so `DJANGO_SECRET_KEY` needs to be copied by hand from the web service's generated value into the worker's - noted inline in the file and in the README, currently harmless since the worker never signs/verifies cookies or tokens.
  - **Smoke-tested locally, not just built**: built the `production` target directly (`docker build --target production`), ran the resulting image against the existing local Postgres/Redis, and confirmed live: a plain HTTP request redirects to HTTPS (expected default), a request with `X-Forwarded-Proto: https` (simulating Render's proxy) does *not* redirect and serves `/health/` correctly, and a static file (`/static/admin/css/base.css`) is served by whitenoise with a 200. This is the strongest verification possible without an actual Render account.
  - **README.md** gained a "Deployment (Render)" section with the concrete human steps (connect the repo via Render's Blueprint flow, provide the `sync: false` secrets, run migrations + `load_destinations` once live, add a custom domain later). Also fixed a stale instruction found in passing: the README's Docker test command was still missing the `-e DJANGO_SETTINGS_MODULE=config.settings.test` override the Phase 15 investigation established as required.
  - **Update 2026-08-30 (same day): the user did the deploy - and it took real debugging to get right.** The user's actual first attempt didn't go through the Blueprint flow at all - a single service ("wanderes") was created manually, so none of `render.yaml`'s automatic resource wiring ever applied to it. That service did surface one real, independent bug immediately (bare `/` 404'd - fixed with a redirect to `/chat/`, `config/urls.py`), but its `/health/` stayed persistently "database unavailable". Diagnosed collaboratively: added logging to reveal the real error (was silently swallowed), confirmed `DATABASE_URL` was never set because no Postgres/wiring existed for this manually-created service, and had the user delete it and redo it properly via "New > Blueprint" (this time all 4 resources - web, worker, db, redis - were created). That redeploy immediately passed `/health/` and the root redirect, but surfaced three more real bugs: the worker crash-looped on `production.py`'s `DJANGO_ALLOWED_HOSTS` fail-fast check (a Celery worker has no HTTP requests and no `RENDER_EXTERNAL_HOSTNAME` to satisfy it - fixed with an explicit placeholder value), then kept failing from an OOM (Celery defaulted to 16-way concurrency on a 512MB plan - fixed with `--concurrency=2`), and `load_destinations` failed because the curated dataset lived inside `documentation/`, which `.dockerignore` excludes from the image entirely (invisible locally because `docker-compose.yml` bind-mounts everything) - fixed by moving it to `travel/data/curated_destinations.json`, its proper home as real application data, with new regression tests since the command had none before. App is live at https://travelagent-web.onrender.com/. Full root-cause writeup in `DEVELOPMENT_LOG.md`. 168/168 tests passing.
  - **Still not done, correctly so**: everything the guide actually means by "Phase 18" itself - talking to real users, watching behavior, collecting qualitative feedback, deciding what to build next based on what's learned. The app being reachable was the prerequisite; that work starts now that it is.
  - **Two more bugs found via real usage on the live deploy, fixed same day**: (1) the intent classifier got "stuck" repeating `off_topic` for clearly travel-related follow-up messages ("quero viajar", "i want to travel") after an earlier message in the same conversation was genuinely off-topic - conversation memory (shipped earlier the same day) fed it full history with no instruction to judge each message on its own content, so it anchored on the established pattern instead of re-evaluating. Fixed by explicitly instructing the classifier to judge only the current message, and to treat short plain travel-intent expressions as `recommendation`, not `off_topic`. (2) The user asked directly for multi-language support - `ai/prompts.py` and the intent-extraction prompt now instruct the model to reply in whatever language the traveler is writing in. **Known, accepted gap**: the fixed Python-string canned replies (off-topic/no-matches/fallback/login-required/feedback acks) stay English - they're deliberately not AI-generated (a past cost decision), so they can't dynamically match language without a bigger change; flagged to the user, no action taken yet. Verified live: replayed the exact reported conversation, confirmed Portuguese and English replies both correctly matched per-message. 168/168 tests unchanged (pure prompt tuning, no new deterministic code path to test).
  - **Dead-end "no matches" reply replaced with real AI reasoning**: reported live - a search that matched zero curated destinations (hard constraints too narrow) returned a fixed canned message ("relax a constraint") instead of trying to help. Per the same Phase 11 recommendation philosophy already approved for vibes the deterministic model doesn't cover, and per the user's direct instruction, this now makes a real AI call: the model either suggests 1-3 real destinations from its own general knowledge (clearly flagged as not from our verified data) or asks a genuine clarifying question, whichever the message actually calls for - its judgment call, not a fixed rule. `NO_MATCHES_REPLY` removed (no longer used). Refactored the shared streaming/fallback/memory-saving logic in `ai/orchestration.py` into `_stream_ai_reply()`, used by both this path and the normal recommendation-explanation path, rather than duplicating it. 1 existing test updated to match the new behavior. 168/168 passing, `ruff check .` clean. Verified live against the real OpenAI API: a genuinely unmatchable request (45°C beach in July) now gets a real, warm clarifying question in Portuguese instead of a dead end.
  - **"Help me decide" no longer loops asking for month forever**: reported live - a traveler who explicitly said "não sei o mês ainda, me ajude a decidir" (I don't know the month, help me decide) kept getting asked for the month again anyway, since nothing distinguished "hasn't answered yet" from "explicitly declines to answer and wants the app to decide". Added a new `flexible_month` field to the intent-extraction schema/prompt - when the traveler says this (in any language, any phrasing), the app substitutes the current calendar month (`date.today().month`) instead of re-asking, and tells the explanation call to mention transparently that a month was assumed. Also made this "stick" across later messages in the same conversation that move on to other details (trip_type, family, budget) without repeating "I don't know" - otherwise it reset every turn. 1 new regression test, 169/169 passing, `ruff check .` clean. Verified live by replaying the exact reported conversation: the assistant now proceeds with real recommendations (Lisbon/Kyoto/Cape Town, then Zanzibar once beach+family was added) instead of looping, correctly mentioning "considerando que estamos no mês 8" (assuming the current month) each time.
  - **Chat got permanently stuck re-asking already-answered questions (a real severity escalation from the above)**: reported live with a full transcript - a conversation gave beach/family-of-4/December/low-budget/"quente e relaxante" and the assistant never once produced a recommendation, endlessly re-asking for information already given. Root cause, found by tracing the conversation against the real API with intermediate state printed at every step: the intent-extraction call (`ai_provider.generate_structured_reply()`) had no `temperature` set, defaulting to OpenAI's high-randomness 1.0 - calling it twice with the *identical* message and history could return different extracted fields each time, directly undermining every deterministic `needs_clarification` fix made earlier that day (they correctly acted on whatever came back, but what came back wasn't stable turn to turn). **Fixed**: added a `temperature` parameter to the `AIProvider` interface, threaded through the OpenAI adapter, and `_extract_intent()` now calls it with `temperature=0` - the explanation/streaming calls are untouched (variety there is fine). 170/170 tests passing (1 new, confirming temperature reaches the real API call), `ruff check .` clean. Verified live by replaying the exact reported conversation: the catastrophic never-converging loop is gone, real recommendations (Zanzibar) come back at multiple points, and the one genuine no-match turn asks a single reasonable follow-up instead of looping. **Known, smaller remaining rough edge, reported honestly**: one specific turn (adding only `trip_type`, no timing info) can still occasionally re-ask for month once before stabilizing - much less severe than the reported bug (resolves within one more turn, doesn't loop forever) and not chased further in this pass.
  - **Architectural fix, not another patch: the clarification gate is gone entirely.** The user correctly pushed back that every fix above was still "remediando" (patching) rather than addressing the real problem - the AI "parece que ta so seguindo um roteiro" (seems to just be following a script) instead of reasoning, and needs "a capacidade de trazer resultados quando nem todos os criterios forem cumpridos, deixando esse criterios como nao relevantes" (the ability to bring results when not all criteria are met, treating those criteria as not relevant). Root cause: `month` was still the one field allowed to gate the whole pipeline (`needs_clarification`) before any search was attempted - every other field was already "unspecified = not relevant", month was the exception. **Removed structurally, not just discouraged**: `needs_clarification`/`clarification_question`/`flexible_month` deleted entirely from `INTENT_SCHEMA` - OpenAI's strict JSON schema mode means the model literally cannot emit a field that isn't in the schema, so "ask instead of answering" is now impossible, not just less likely. `month` missing/invalid unconditionally defaults to the current month (no more conditional flexible-month branch). The no-matches AI fallback no longer offers "ask a clarifying question" as an equal option - it always tries to suggest from general knowledge, relaxing whichever constraint made the curated data empty. `needs_clarification` removed entirely from `OrchestrationResult`/`StreamingOrchestrationResult` (nothing outside this module ever read it, and it would always be unused now). 168/168 tests passing (2 obsolete regression tests removed since their whole premise no longer exists, others rewritten), `ruff check .` clean. **Verified live against the real OpenAI API by replaying the exact reported conversation from scratch**: every single message now returns real recommendations (18, 4, 1, 1, 1) - including the final one, which hits every hard constraint at once and genuinely matches nothing in the curated dataset, yet still returns a real general-knowledge suggestion ("Praia do Forte, Brasil") instead of another question. Zero clarifying questions anywhere in the replay.
  - **Second round of the same feedback - two response paths still weren't AI-generated at all.** The user found this immediately when trying the fix above: a bare "oi pode me ajudar com uma viagem?" (nothing else - no month/climate/budget) still jumped straight to 3 specific destination suggestions ("it's not supposed to just start giving data, it's supposed to try to gather information first"), and "vc é uma ia?"/"quantos anos vc tem?" both got the identical verbatim off-topic string ("doesn't feel like an AI, just an if/else that can't reason outside the pipeline"). Explicit direction: make it behave like ChatGPT/Grok/DeepSeek, focused on travel, only redirecting when something is genuinely unrelated. **Fixed**: `SYSTEM_PROMPT` (`ai/prompts.py`) rewritten to license real reasoning - ask genuine follow-ups when there's not enough to suggest something good yet, answer brief questions about the assistant itself honestly, only redirect (in its own words, never the same sentence twice) when truly unrelated to both travel and itself. `off_topic` no longer returns the fixed `OFF_TOPIC_REPLY` (removed) - it now makes a real AI call. A message with *nothing* extracted at all (not even an explicit month) no longer runs an unfiltered search and jumps to suggestions - it now hands the actual "ask vs. suggest" judgment to a dedicated AI call instead of a Python rule. The Phase 11 "relying on AI judgment over the full candidate list" logging (for *some* signal given, e.g. an explicit month with no scoring match - "a romantic getaway in June") was kept as its own separate case, since that's meaningfully different from nothing given at all. 169/169 tests passing (2 off-topic tests rewritten since their old premise - zero AI calls - is exactly what changed; 1 new test for the open-ended path), `ruff check .` clean. **Verified live against the real OpenAI API replaying the actual exchange**: the bare opener now asks a genuine varied question instead of listing destinations; "are you an AI?" and "how old are you?" get different, honest, contextually appropriate answers instead of an identical canned string; asking something truly unrelated ("capital of France?") got answered briefly and naturally before steering back to travel on its own - closer to how a real assistant behaves than a hard refusal.
- [x] **Out-of-sequence: site-wide CSS/JS pass** ✅ Done 2026-08-30 - not a numbered guide phase, a direct user request after being told honestly that only `/chat/` had any styling (every other page - auth, profile, trips, history, feedback - was bare unstyled Django form/list output). Asked to make the whole site "presentável e usável" (presentable and usable), not a full redesign - stayed within `08_FRONTEND_ARCHITETURE.md`'s existing "Django templates + HTML + CSS + light JS, no React" scope.
  - `static/css/main.css` (new, ~300 lines): one shared stylesheet - CSS custom properties for color/spacing, a persistent header with site nav (auth-aware: shows Chat/My trips/My history/Profile/Account/Log out when authenticated, Chat/Log in/Register when not), styled forms/buttons/messages/lists, all targeting the *existing* plain HTML (`{{ form.as_p }}`, bare `<ul>`) rather than requiring every template to be rewritten - most templates only needed a class name or two added (`btn`, `btn-danger`, `item-list`), not restructuring.
  - `static/js/main.js` (new, small): a mobile nav toggle and dismissible messages - deliberately kept to "light JS", no framework.
  - `config/settings/base.py` gained `STATICFILES_DIRS` so Django's static finder picks up the new `static/` source directory (previously only `STATIC_ROOT`/`STATIC_URL` existed, for `collectstatic`'s output).
  - `templates/base.html` restructured: added the header/nav, static asset `<link>`/`<script>` tags, Django message `tags` now render as a CSS class (enables success/error/warning styling), and a new `content_class` block so most pages get a shared readable-width wrapper (`.site-content`) while a page that manages its own full-height layout can opt out.
  - **A real regression risk, caught and handled deliberately, not accidentally avoided**: `/chat/`'s carefully mobile-viewport-tuned layout (fixed back in Phase 10 - `documentScrollHeight` had been ~2123px instead of 812px on mobile before that fix) used `height: 100vh` on the assumption that it was the *only* thing on the page. Adding a persistent header+footer around every page, including chat, would have silently broken that exact fix by making the total page taller than one viewport again. Fixed by having chat override the new `content_class` block to a flex-based `.content-flush` wrapper instead, and changing `.chat-page` from `height: 100vh` to `flex: 1 1 auto` (fills whatever space the flex layout actually leaves, not a fixed viewport fraction) - verified this concretely afterward via `javascript_tool` against the real running page: `document.documentElement.scrollHeight` (812) now exactly equals `window.innerHeight` (812) on an emulated mobile viewport, confirming zero page overflow.
  - **A second real bug found and fixed during this same pass, unrelated to the CSS/JS work itself**: `whitenoise.storage.CompressedManifestStaticFilesStorage` (added to `base.py` in the Phase 18 deploy-prep commit) requires `collectstatic` to have already generated a hashed manifest file - true only in the `production` Docker stage, never in local dev/test. The full test suite failed 23/149 the moment templates started referencing `{% static %}` tags, with `ValueError: Missing staticfiles manifest entry`. **Fixed properly, not by suppressing the error**: moved the manifest storage override out of `base.py` (which now defaults to Django's plain `StaticFilesStorage`, needing no manifest) and into `config/settings/production.py` only. This surfaced a second, deeper bug in the same area: the `Dockerfile`'s `collectstatic` step deliberately ran under `development` settings to dodge `production.py`'s fail-fast `ALLOWED_HOSTS`/`SECRET_KEY` checks - but that meant it would have generated its manifest under the *plain* storage (since only `production.py` now has the manifest one), leaving the real runtime process with no manifest to find, reproducing the exact same `ValueError` at real deploy time instead of at test time. Fixed by running `collectstatic` under `production` settings with throwaway build-time-only `DJANGO_ALLOWED_HOSTS`/`DJANGO_SECRET_KEY` values instead, satisfying the fail-fast checks without needing real ones. Verified by rebuilding the `production` target and confirming, in a real running container, that `{% static %}`'s hashed URL (`/static/css/main.<hash>.css`) resolves with a real `200`.
  - 149/149 tests still passing (no test changes needed - none asserted on the previously-bare HTML structure in a way the new classes broke), `ruff check .` clean.
  - **Verified live in a real browser** (not just automated tests): desktop nav (auth-aware, both logged-in and logged-out states), the mobile nav toggle (confirmed via direct DOM inspection - `is-open` class and `aria-expanded` toggle correctly), a real registration → create-trip → account flow with the new styling, and the mobile chat viewport fit described above. Replaced an initial Unicode hamburger-icon glyph (`&#9776;`) with a small CSS-drawn icon after noticing it could render as an empty/tofu box depending on font availability - a plain three-`<span>` bars icon has no such dependency.
- [x] **Out-of-sequence: chat bug fixes + conversation memory** ✅ Done 2026-08-30 - three closely-related pieces of work, all from real conversations the user had via the newly-styled chat. Full root-cause writeups in `DEVELOPMENT_LOG.md`; summary:
  - **Login-wall dead-end fixed**: an anonymous user answering a month-clarification question with "someday between september and october" got hard-blocked with a login wall - the intent classifier misread "someday" as `future_intent` with no destination at all. Fixed the classifier prompt (`future_intent` now requires an actual destination) and reordered `_handle_feedback`/`_handle_future_intent` to check for a destination before checking login, in both functions - defense-in-depth so a future misclassification can't do this again. Also fixed a related gap: the classifier had no rule for a range of two months ("September and October"), leaving `month` null.
  - **Real conversation memory added**, Redis-backed, per direct user request: `ai/memory.py` (new) stores the last 6 turns per conversation (keyed by account for authenticated users, by Django session for anonymous ones) in the existing Redis-backed cache - no new infrastructure. Every branch of `stream_travel_recommendation()` now saves its turn and feeds prior turns into intent extraction, closing the "each message classified in total isolation" gap that the login-wall bug was really a symptom of. New `conftest.py` clears the cache between tests (the DB resets between tests automatically; the cache does not - a real test-isolation gap caught while adding this).
  - **Clarification loop fixed**: the model kept asking about `trip_type` (optional) as if required, looping forever even after the user answered twice. `_validate_intent()` now decides `needs_clarification` in code - `True` only when `month` (the only field the pipeline actually requires) is genuinely missing, `False` whenever a valid month is present, regardless of what the model itself set. The classifier prompt was also rewritten for warmth (acknowledge what the traveler already said, never phrase a question as a list of categories, never repeat an already-answered clarification question - using conversation memory to know it already asked).
  - 165/165 tests passing (16 new since the CSS/JS pass: memory unit tests, conversation-memory-in-orchestration tests, the login-wall and clarification-loop regression tests), `ruff check .` clean. All three fixes verified live against the real OpenAI API by replaying the actual reported conversations, not just via mocked tests.
- [x] **Out-of-sequence: real production deploy completed by the user, then three more chat/CSS bugs fixed** ✅ Done 2026-08-31. The user completed the actual Render Blueprint deploy themselves (per Phase 18's "Human" ownership); Claude Code diagnosed a series of live production issues from pasted logs (see `DEVELOPMENT_LOG.md`'s Render entries) - all resolved, the site is fully live and healthy. After deploy, the user reported (verbatim, with a full transcript) that the AI still "keeps throwing destinations at the user's face" even after the previous session's redesign, plus that the chat input bar wasn't staying visually pinned (had to scroll to find it). Three fixes, all verified live against the real OpenAI API and the real rendered page, not just mocks:
  - **CSS input-bar bug**: root cause confirmed empirically (simulated a long conversation in the browser and measured real DOM heights) - `body { min-height: 100vh }` in `static/css/main.css` let the whole page grow past one viewport instead of capping it, so the intended "internally-scrolling `.chat-messages`, pinned `.chat-form`" flexbox chain never actually got a bounded box to squeeze into; `.chat-messages` never scrolled internally at all (`scrollHeight === clientHeight`). Fixed by giving `body` an actual `height: 100dvh` (kept `min-height: 100vh` alongside it as a fallback for browsers without `dvh` support) - `.content-flush`/`.chat-messages` already had the correct `min-height: 0`/`flex`/`overflow-y: auto` chain, they just never had a real ceiling to work within. Verified both effects hold: a simulated 40-message chat now stays within one viewport with the input bar visible (desktop and mobile viewport both checked), and a normal long content page (`/trips/`) still scrolls the whole page exactly as before - the fix only bites where `min-height: 0` is set (chat page), not elsewhere.
  - **"Still throwing destinations" bug, root cause found**: `stream_travel_recommendation()` (`ai/orchestration.py`) had two separate checks - `has_any_signal` (gated whether to search at all) and `no_scoring_signal` (only logged, never gated) - and a bare month (stated or defaulted) or an exclusion alone satisfied `has_any_signal`, letting the pipeline run a real, essentially-unfiltered search and then explain whatever came back. That's exactly what produced Rio/Lisboa/Tóquio off "help me plan my year-end trip" and Algarve/France/Costa Rica off "something for family" - `trip_type`/`min_temp_c`/`max_cost_of_living` (the fields that actually differentiate one destination from another) were never checked as a group. Fixed by merging both checks into one: only `trip_type`/`min_temp_c`/`max_cost_of_living` gate a real search now: month/exclusions alone route through the same AI-judgment "ask a genuine follow-up, or suggest if explicitly invited to guess" path already used for a fully blank opener (`_build_open_ended_messages()`, now also given `intent` so it can name what's already known - e.g. a stated month - instead of re-asking about it).
  - **`future_intent` misclassification fixed**: "mudei de ideia quero abril do ano q vem" (changed my mind, want April next year) - no destination named at all - was misclassified as `future_intent`, producing "which destination did you have in mind?" instead of acknowledging the month change. `INTENT_EXTRACTION_SYSTEM_PROMPT`'s `future_intent` rule required "no destination" cases to fall back to `recommendation`, but only explicitly covered timing-only phrasing, not desire/intent language ("quero", "mudei de ideia") without a destination. Prompt rule broadened to cover both explicitly.
  - 169/169 tests passing (one test's log-message assertion updated for the merged check's new log line; `_build_open_ended_messages()`'s new `intent` parameter threaded through its one caller and test), `ruff check .` clean. **Verified live against the real OpenAI API**, replaying the exact reported conversation: "quero que me ajude a montar minha viagem de fim de ano" now asks a genuine follow-up instead of listing destinations; "mudei de ideia quero abril do ano q vem" correctly acknowledges the change and asks about trip type instead of asking for a destination; "quero algo em familia" asks what kind of activity instead of naming three places. A follow-up "surpreenda-me, escolha voce" (explicitly inviting a guess) correctly *does* suggest real destinations - confirming the AI-judgment path works both directions, not over-corrected into always asking.
- [x] **Out-of-sequence: requested AI-intelligence testing pass, one bug found and fixed** ✅ Done 2026-08-31 - the user asked for broad live testing of the AI's reasoning ("ela ainda parece confusa"), not one specific reported bug. Ran ~6 live multi-turn scenarios against the real OpenAI API (multi-criteria-in-one-shot, gradual build-up, mid-conversation correction + language switch, feedback + exclusion, self-referential interruption). Most came back clean - corrections, language switching, exclusions, and off-topic interruptions all worked with no repeated questions or lost context.
  - **One real bug found and fixed**: `min_temp_c` was being silently inferred as `28` for messages that never mentioned temperature at all - specifically a trip_type + month combination like "praia em julho" (beach in July), while "praia" alone did not trigger it. Traced by calling `_extract_intent()` directly with hand-crafted inputs - fully consistent/deterministic (temperature=0 working as intended), so this was a genuine prompt-following gap, not sampling noise, and not the initially-suspected "conversation history leaking the assistant's own prior temperature mentions" (that reproduced with empty history too - a red herring). `INTENT_EXTRACTION_SYSTEM_PROMPT` strengthened in `ai/orchestration.py` with an explicit statement that trip_type/destination never implies temperature or budget, plus (needed for the fix to actually take, per live re-testing) a concrete worked counter-example naming the exact "beach in July" failure pattern - the abstract rule alone wasn't enough for gpt-4o-mini to reliably follow. Verified live after each change: "praia em julho" now correctly leaves `min_temp_c` null while "quero uma viagem de praia bem quente" (explicitly hot) still correctly sets it to 28. 169/169 tests passing (this class of bug - a live model not following a prompt instruction - isn't one the mocked test suite can catch; verification here was necessarily live).
  - **Second finding, raised as a product decision, then implemented per the user's choice**: `_handle_feedback()`/`_handle_future_intent()`'s templated acknowledgment strings were hardcoded English regardless of conversation language (deliberately non-AI, 2026-08-29, to avoid a second AI call per message) - this predated and conflicted with the later "the site should respond in more than one language" request. Presented three options; **the user chose to accept a small extra AI call** over a limited per-language template table or leaving it as-is. Implemented as `_localize_reply()` (new in `ai/orchestration.py`) - one small non-streaming AI call that phrases an already-decided fact in the traveler's language, never asked to decide anything itself. 169/169 tests passing (`StubAIProvider`/`FailingAIProvider` gained `generate_reply()`), `ruff check .` clean. **Verified live**: feedback/future-intent/login-required confirmations in a Portuguese conversation now come back fully in Portuguese instead of the old hardcoded English sentences. See `DEVELOPMENT_LOG.md` for the full writeup.
- [x] **Out-of-sequence: another live-reported conversation - a spurious login wall, a misclassification, and a real architectural gap** ✅ Done 2026-08-31. User pasted a full transcript showing "Bahia" (picking one of the assistant's own just-listed suggestions) triggering a login wall, and "5 piores no contexto geral" getting misrouted into the feedback handler's "which destination" dead-end.
  - **Login wall root cause**: `future_intent`'s classifier rule technically matched "Bahia" (a named place) even though the user was just picking from the assistant's own prior suggestions, not spontaneously raising a someday-goal. Fixed with a concrete rule for exactly this pattern in `INTENT_EXTRACTION_SYSTEM_PROMPT`, plus tightened `feedback`'s definition (must show past-tense evidence of the traveler's own visit - a request to rank/evaluate places is never feedback) and a general "prefer 'recommendation' when genuinely ambiguous" fallback rule.
  - **Deeper issue found while debugging**: "Bahia"'s reply came back in English mid-Portuguese-conversation, reproducibly. Root cause: conversation history was only ever passed into the intent-*extraction* call, never into any of the reply-*generation* calls (`_build_explanation_messages` and three siblings, plus `_localize_reply`) - each saw only the current message in total isolation, with no conversation-level signal for language or continuity. Fixed by threading `history` through every AI-call builder in `ai/orchestration.py` via a new shared `_history_messages()` helper - likely relevant to more than just this one language slip, since it's the first time the actual reply-generating calls have seen the conversation at all.
  - New regression test locking in that the explanation-path reply call receives prior history, not just extraction. 170/170 tests passing, `ruff check .` clean. **Verified live**, replaying the exact reported conversation: "Bahia" now gets a real Portuguese, on-topic reply continuing the search - no login wall, confirmed stable across repeats. See `DEVELOPMENT_LOG.md` for the full writeup.
- [x] **Out-of-sequence: direct product request - gather more before suggesting, as a friendly emoji quiz** ✅ Done 2026-08-31. User: "ele precisa tentar pegar mais informações antes de tentar sugerir algo... faz tipo um questionario bonitinho com emojis de praia neve, viagem etc" - after "gosto de praias" (trip_type alone) still jumped straight to suggestions.
  - Raised the search-gate bar in `stream_travel_recommendation()`: `min_temp_c`/`max_cost_of_living` alone remain sufficient (rarely stated without real intent), but `trip_type` alone no longer is - only counts once paired with an explicit month. Renamed `has_differentiating_signal` → `has_enough_signal` to match.
  - `_build_open_ended_messages()`'s prompt now explicitly asks for a warm, quiz-style message with relevant emojis (trip type/timing/budget together, adapted to what's already known) instead of a plain question, when early in the conversation - checked against history so it doesn't repeat itself.
  - **Third bug found live while verifying**: answering the assistant's own new quiz ("gosto de praias" right after it) caused `min_temp_c` to be spuriously inferred as 28 again - a *new* variant of the earlier same-day temperature-inference bug: the model was treating an answer to one part of a multi-part question (trip type) as implicitly answering the other parts (climate) too, since the question itself discussed climate. Extended `INTENT_EXTRACTION_SYSTEM_PROMPT`'s history-poisoning guidance to cover this explicitly.
  - 170/170 tests passing, `ruff check .` clean. **Verified live end-to-end**: quiz message with emojis appears correctly, "gosto de praias" now asks only about the missing pieces instead of suggesting, and a third turn with real signal ("dezembro, orçamento médio") correctly returns a real curated recommendation. Temperature-inference fix re-confirmed stable across repeats. See `DEVELOPMENT_LOG.md` for the full writeup.
- [x] **Out-of-sequence: no-matches fallback reply tone fixed - stop apologizing before helping** ✅ Done 2026-08-31. User reported "queria esquiar" getting general-knowledge ski suggestions prefaced with "Infelizmente, não tenho dados específicos..." - clarified (after an initial deploy-timing false alarm on my part) that the actual complaint was the apologetic "I don't know" opener itself, not that it fell back to general knowledge at all (that pivot was already correct/intended). `_build_no_matches_messages()` and `_build_open_ended_messages()`'s "explicitly invited to guess" branch both rewritten to explicitly forbid opening with an apology/data-admission - lead with confident help, mention the general-knowledge caveat briefly in passing instead. No test changes (pure live-model-tone concern). **Verified live**: the ski no-matches reply now opens with "Existem alguns excelentes destinos onde você pode esquiar..." confidently, confirmed stable across repeats. See `DEVELOPMENT_LOG.md`.
- [x] **Out-of-sequence: English conversations answered in Portuguese/Spanish - a regression from this session's own wording** ✅ Done 2026-08-31. A full English conversation ("Hi" / "i wanto to travel..." / "I wanna go to a snowy place") got Portuguese (once reproduced, Spanish) replies from the quiz-gathering path onward. Root cause: `SYSTEM_PROMPT`'s language rule ("do not default to English if they wrote in another language") was asymmetric - only ever warning against defaulting *to* English, never away from it - which combined with the same-day "judge language from the whole conversation" addition to bias the model toward avoiding English specifically when inferring language from context. Fixed in two parts: `SYSTEM_PROMPT` rewritten to be fully symmetric across all languages (didn't fully fix the quiz path alone); a local, explicit "reply in the language the traveler has been using - this applies to English too" reminder added directly inside four of the five per-call prompt builders (`_build_explanation_messages`, `_build_open_ended_messages`, `_build_no_matches_messages`, `_localize_reply`) - only the local reminder actually fixed the quiz path, confirming a pattern already seen this session: rules stated once at a distance are measurably less reliable than the same rule reinforced right at the point of generation. 170/170 tests passing (one test stub's marker string updated to match new wording), `ruff check .` clean. **Verified live**, replaying the exact reported conversation twice (extended to 4 turns including a real curated recommendation) - consistently English throughout both times, versus consistently non-English before. See `DEVELOPMENT_LOG.md`.

- [x] **Out-of-sequence: direct product request - comparison tables instead of paragraphs, keep refining after suggesting** ✅ Done 2026-09-01. User: "quero que crie tabelas de comparação entre destinos, ou de pros e contras... em situações como essa, ele sugere mas tbm pede mais informações para aprimorar a busca" - illustrated with plain numbered-paragraph suggestions and no real refining question.
  - **Real architectural gap found first**: the chat UI rendered every reply as plain `textContent` - a Markdown table would have shown as illegible raw pipe/dash text. Added a small dependency-free Markdown-to-HTML renderer to `ai/templates/ai/chat.html` (bold, lists, GFM-style tables only - not general Markdown), HTML-escaping first so only explicitly-parsed constructs can ever produce markup. Only the assistant's streamed reply uses it (`innerHTML`); user/loading/error bubbles stay plain text. New CSS for tables/lists inside assistant bubbles, with a horizontal-scroll wrapper so wide tables don't break mobile layout.
  - Backend prompts (`SYSTEM_PROMPT`, `_build_explanation_messages`, `_build_no_matches_messages`, `_build_open_ended_messages`'s guess-branch) now explicitly ask for a compact Markdown comparison table (destination/climate/cost/pro/downside, adapted per case) instead of numbered prose, plus a genuine narrowing follow-up question after suggesting - not a fixed rule in Python, a prompt instruction.
  - No test changes needed (pure prompt content; no frontend JS test suite exists in this project's scope). 170/170 backend tests passing, `ruff check .` clean. **Verified live in a real browser**: real HTML table rendered (Queenstown vs. Cusco/Machu Picchu comparison) with a genuine refining question after it; confirmed mobile horizontal-scroll works; confirmed plain conversational replies and "Save as trip" links still work unaffected. See `DEVELOPMENT_LOG.md`.

- [x] **Out-of-sequence: Flight & Hotel Affiliate Provider research (Phase 23 material)** ✅ Research done 2026-09-01, decision still pending. User explicitly requested this ahead of Phases 19-22 (not yet started) - research and documentation only, no code, matching Phase 23's "Human Decision + Research" ownership.
  - Researched official docs/support pages + independent sources for Skyscanner, KAYAK, Kiwi.com (Tequila), Amadeus for Developers, Aviasales/Travelpayouts, Duffel, and Booking.com. **Headline finding**: every genuine real-time flight/hotel search API gates access behind a traffic threshold (typically 50k-100k+ monthly users) Wanderes doesn't have pre-launch - this is an industry-wide pattern, not a Wanderes-specific gap. Notably, Amadeus for Developers' historically-standard free self-serve tier was **decommissioned 2026-07-17** (very recent, confirmed via independent trade press, not just a vendor page).
  - **Two providers are actually reachable right now**: Duffel (flights - self-serve, no traffic minimum, but a book-through-API/hosted-checkout model rather than pure affiliate redirect - a real business-model difference, not just a vendor choice) and Booking.com's Affiliate Partner Program (hotels - open to "travel bloggers," real XML data feed with live pricing once approved).
  - Full comparison tables, the "two architectures" framing, and Claude Code's recommendation (not a decision) are in `DECISIONS_PENDING.md` §4. Technical shape (interfaces, normalized `FlightOption`/`HotelOption`, commission-independence requirement, affiliate tracking via the existing `analytics` app, caching guidance) documented in `10_EXTERNAL_INTEGRATIONS.md` §13, ready to implement once a provider is chosen. `14_MVP_IMPLEMENTATION_PLAN.md` updated with a short status note at Milestone 3.
  - Nothing implemented - explicitly out of scope for this pass per the user's own instructions ("do not implement... until the provider has been explicitly approved").
  - **A follow-up Duffel-specific decision + `FlightProvider` design was made and then reverted the same day** at direct user request ("desfaz tudo relacionado ao duff") - `git revert` of that commit, safe since nothing had been implemented yet. Flight provider selection is fully open again; see `DEVELOPMENT_LOG.md`'s "Duffel flight-provider decision reverted" entry for the full account.
- [x] **Custom domain `wanderes.com` added to production `ALLOWED_HOSTS`** ✅ 2026-09-01. Set via `render.yaml`'s `DJANGO_ALLOWED_HOSTS` env var (`wanderes.com,www.wanderes.com`), not hardcoded in `production.py` - matches the existing environment-driven config pattern already used for every other per-environment value. `CSRF_TRUSTED_ORIGINS` derives from this automatically, no separate change needed.
- [x] **Out-of-sequence: UX/UI polish pass - landing page, visual identity, login/register redesign, recommendation cards** ✅ 2026-09-01. Detailed brief requesting presentation/usability improvements only - architecture, backend behavior, AI behavior, and database untouched, no new frontend framework or dependency.
  - **Real landing page** at the bare domain root, replacing the old `RedirectView` straight to `/chat/` - `core.views.landing` (new), features 3 real curated destinations (never fabricated copy), links to chat as the primary CTA. `core/tests/test_root_redirect.py` (tested behavior that no longer exists) replaced with `core/tests/test_landing.py` (3 new tests).
  - **Visual identity refresh** (`static/css/main.css`, `templates/base.html`): added Fraunces (warm editorial serif for headings, loaded via `<link>` not `@import` for performance) + Inter, a warm accent color alongside the existing teal primary, a shadow/radius scale used consistently across buttons/cards/inputs. Existing color *values* left untouched - only additive.
  - **Login/register pages redesigned** as centered `.auth-card` components instead of a bare `{{ form.as_p }}` - no Django form logic touched, layout only.
  - **Recommendation cards**: chat page now shows real structured cards (name, country, trip type/cost tags, real avg-high-°C, a "why this fits you" list) instead of bare "Save as trip" links. Backend enrichment only, no fabrication - `ai/views.py`'s new `_recommendation_card_data()` exposes fields `ScoredDestination` already computed (`trip_type`, `cost_of_living`, `avg_high_c`) and translates existing `preference_fit`/`budget_fit`/`temperature_fit` scoring factors into safe user-facing strings, never exposing AI reasoning or raw scores.
  - Contextual loading messages (picked at random from 4, replacing the static "Wanderes is thinking..."), stale page titles (still saying the project's old name at the time) fixed site-wide, header brand link now points to the landing page instead of straight to chat.
  - Flight/hotel UI presentation (brief §13) intentionally not built - no real provider connected yet (Duffel decision was reverted the same day, see above), so there's nothing real to present without fabricating data.
  - 173/173 tests passing (5 new/updated), `ruff check .` clean. **Verified live in a real browser**: landing page (real destinations, correct centering confirmed via DOM measurement after a screenshot-tool rendering artifact), login/register cards, a real chat exchange producing 5 real recommendation cards against the live OpenAI API, mobile viewport (375px, no horizontal overflow, cards/grids collapse correctly). See `DEVELOPMENT_LOG.md` for the full writeup and what was deliberately left unchanged.
- [x] **Out-of-sequence: second UX/UI pass - fixed a real production bug, added the product preview** ✅ 2026-09-01. Follow-up brief reviewing the actual live site (`https://www.wanderes.com/`), not just local code, against the first pass's goals.
  - **Real production bug found and fixed**: the site footer rendered visually on top of the landing page's destination-teaser heading. Root cause: `.content-flush` (built for chat's internal-scroll region) was reused for the landing page, which has no such region - its `min-height:0` capped `<main>` to a flex-computed height shorter than its actual content, and since overflow wasn't clipped, the overflowing content rendered on top of the next flex sibling (the footer) instead of pushing it down. Split into `.content-flush` (chat only, needs internal scroll) and a new `.content-wide` (landing/login/register - simply taller than one viewport, scrolls normally). Confirmed via live DOM measurement on production before fixing, and again locally after.
  - **Product preview added to the landing page** (brief's most emphasized correction: "show the product, not just describe it") - a real `.chat-bubble` + `.recommendation-card` example using a real curated destination, not a disconnected marketing mockup. Required moving those component styles from `chat.html`'s local `<style>` into shared `main.css` first, since both pages now use them - a real fix for "repeated CSS that should share a common style," not scope creep.
  - **"Why this fits you" gained a visually separate label** in both the real chat cards and the new preview, so the personalized rationale reads as clearly distinct from the destination's own facts.
  - **Chat empty state**: previously a large blank void before the first message; added three clickable example-prompt chips (fill the input, don't auto-send) - functional, not decorative, kept deliberately small.
  - Tightened `.landing-section`'s padding after noticing real excess whitespace on the live page.
  - 173/173 tests passing, `ruff check .` clean. **Verified live**: the original bug's repro and fix both confirmed via DOM measurement (production and local); product preview, empty-state chips, and the full chat→recommendation flow all re-tested end to end; mobile (375px) re-checked for both pages. See `DEVELOPMENT_LOG.md` for the full writeup.
  - **Not addressed this pass** (flagged, not fixed): `/trips/`, `/users/account/`, `/users/profile/` still use bare generic form styling - reasonable next-phase target, outside the first-time-visitor journey this pass prioritized.
- [x] **Out-of-sequence: third UX/UI pass - ChatGPT-style centered chat, soft login prompt modal, small humanization tweaks** ✅ 2026-09-01. Direct request: centered empty-state chat layout that adopts a standard position once a conversation starts; a dismissible login prompt (not a hard gate) for anonymous chat visitors; general note that the frontend "still feels AI-built."
  - **Centered empty state**: `#chat-page` gains a `chat-empty` class (removed by JS on first sent message) that vertically centers the header/greeting/suggestion chips/input; snaps to the existing top-pinned/internal-scroll layout the moment a real conversation starts.
  - **Login prompt modal**: native `<dialog>`, shown once per browser session (`sessionStorage`) to signed-out visitors only, with Log in / Create an account / "Continue without an account" - chat still works fully anonymously either way. Found and worked around a real browser quirk: `dialog.close()` doesn't reliably fire the `"close"` event when called programmatically - fixed with a shared `markSeenAndClose()` called directly from every dismissal path instead of relying on the event.
  - **Humanization**: two landing-page icon swaps (📍→🧭, ✓→💡) and warmer chat empty-state copy - a small, concrete first pass on an inherently subjective request; more specific direction welcome for further work here.
  - 2 new tests (modal shown/not-shown by auth state) - hit two test false-positives along the way (both from the modal's own CSS/comments containing the literal strings being asserted against, not real bugs), fixed by asserting on `"<dialog"` and rewording a CSS comment. 175/175 total tests passing, `ruff check .` clean.
  - **Verified live, desktop and mobile (375px)**: centered layout, transition to standard layout on first message, modal appearance/centering/dismissal-persistence all confirmed in a real browser.
- [x] **Out-of-sequence: saved conversations (Free-tier limits, ChatGPT-style sidebar)** ✅ 2026-09-02. Direct, fully-specified request: registered users can save chat conversations, max 5 per user / max 50,000 characters each (the exact numbers are the human decision here - CLAUDE.md rule 2 - matching `01_PRODUCT_REQUIREMENTS.md` §6.2's already-documented "Free plan limits" philosophy, just supplying the specific values it had left open). A left sidebar (ChatGPT-style, AI-generated conversation title, click to reload), "New conversation" + a "Save this conversation" checkbox (checked by default, unchecking bypasses saving and both limits entirely for that thread), and one-time modals when either limit is hit - honestly naming a future Premium plan as "coming soon" rather than linking a nonexistent purchase flow (Phase 19 hasn't started).
  - New model **`ai.models.SavedConversation`** (`ai`'s first model - a deliberate, documented departure from its prior "no models" pattern, since this is genuine persistent user-owned storage, not orchestration state) - `user`, AI-generated `subject`, `messages` (JSONField list, same precedent as `Feedback.tags`), `is_full` (one-way flag once the 50k-char limit is crossed).
  - New **`ai/conversations.py`** - `record_turn()` enforces both limits; AI-provider resolution is fully lazy (only constructed when a *new* conversation needs titling) - a real bug caught before shipping: an earlier draft eagerly built a real `AIProvider` on every request, which would have broken every test hitting this view in CI (no `OPENAI_API_KEY` there).
  - `ai/orchestration.py`'s `stream_travel_recommendation()` gained `history_override` - a saved conversation's AI context comes from its own Postgres-stored messages once it exists, not `ai.memory`'s Redis bucket, keeping "conversation context" and "persistent memory" (`09_AI_ORCHESTRATION.md` §7) cleanly separated instead of double-writing.
  - `ai/views.py` gained a second streamed footer (`CONVERSATION_DELIMITER`, same pattern as the existing recommendations footer) plus 4 small JSON endpoints (list/detail/delete/reset), all structurally scoped to `request.user`.
  - **A real live-model bug found and fixed during manual verification**: an English conversation got a Portuguese AI-generated sidebar title, because the underlying destination data's Portuguese country names outweighed the traveler's own wording as a language signal - fixed with an explicit "judge language only from the traveler's own sentence, never a place name" instruction.
  - 24 new tests (8 unit-level `record_turn()`, 16 view/endpoint-level), 199/199 total passing, `ruff check .` clean, one new migration (`ai.0001_initial`).
  - **Verified live end-to-end** against the real OpenAI API and a real (session-only) test user: save/continue/reload/delete, both limits (5 conversations, 50k chars) each firing their modal exactly once and then staying silent, the unchecked-checkbox bypass creating zero DB rows, and the mobile off-canvas sidebar toggle - full account in `DEVELOPMENT_LOG.md`.
- [x] **Site-wide mobile nav: slide-in overlay from the left instead of push-down** ✅ 2026-09-02. Direct request: the header hamburger menu previously pushed page content down when opened; now slides in from the left as an overlay (same off-canvas pattern as the chat sidebar above, `position: fixed` + `transform: translateX`), with a dimmed click-to-close backdrop, and the toggle button itself moved to the left of the "Wanderes" brand. Desktop nav unaffected. No test changes needed (pure CSS/markup). **Verified via direct DOM/CSSOM inspection, not just screenshots** - a real browser-caching artifact in the testing tool (a stale cached `main.js` being executed despite fresh edits, diagnosed via `performance.getEntriesByType("resource")`) and a screenshot-rendering artifact both showed false negatives during verification; the actual served code was confirmed correct via a cache-busted `eval` test. Full account in `DEVELOPMENT_LOG.md`.
- [x] **"Unchecked conversations still saving" - not reproducible, but the requested status indicator was added** ✅ 2026-09-02. Two live tests against a real user (uncheck-before-first-message; uncheck-mid-conversation) both correctly did not save, and code review found no path where `save_requested=False` could still result in a save - flagged to the user as likely a pre-deploy page state rather than a current bug. Added the independently-requested feature regardless: a small "(not saving)" label next to the chat title, next to `<h1>Chat with Wander</h1>`, whenever the next message won't be saved (checkbox unchecked, or the 5-conversation cap silently blocking the thread) - updates instantly on checkbox toggle, not just via the existing one-time modal. Registered users only. No test changes (pure frontend state). Full account in `DEVELOPMENT_LOG.md`.
- [x] **Two live-reported bugs fixed: Monaco dead-ended instead of using AI knowledge; saved-conversation sidebar had a visible refresh delay** ✅ 2026-09-02. (1) `future_intent` for a real destination not in the curated catalog (e.g. "Quero ir pra Monaco") returned a canned "noted, but not in my catalog" reply - a real gap left over from before the Phase 11 general-knowledge philosophy existed, now fixed to match it: a real streaming AI reply using general knowledge, same as an unmatched recommendation request already gets. No Trip is persisted either way (no valid `Destination` row to attach one to - never invent catalog data). (2) The saved-conversations sidebar *was* actually saving correctly the whole time (confirmed via direct DB check) - the bug was a visible network-round-trip delay between save completing and the sidebar re-fetching to show it, which a live check caught mid-flight and read as "not saving." Fixed by inserting the new conversation into the sidebar instantly from data already in the save-status footer, no extra fetch needed. **Also hardened proactively**: `ai.conversations.record_turn()` now never raises (matches `analytics.record_event()`'s established "non-critical work can't break the main flow" contract) - relevant because a fresh deploy's unapplied migration was flagged to the user as the most likely explanation if this is still an issue specifically in production (Claude Code has no production DB access to confirm `python manage.py migrate` was run there since the saved-conversations deploy). 2 new tests, 201/201 total passing, `ruff check .` clean. Verified live replaying the exact reported message. Full account in `DEVELOPMENT_LOG.md`.

- [x] **Full codebase review requested, 7 findings, all fixed same pass** ✅ 2026-09-02. Direct request to review for inconsistencies/logic problems, not a specific bug report. Findings and fixes: (1) `_handle_feedback()` still dead-ended on an uncataloged destination, inconsistent with the Monaco fix earlier the same day - now uses real AI general knowledge, mirroring `_handle_future_intent()`. (2) `RecommendationRequest.max_temp_c` was dead code (never populated by the AI layer) - "somewhere not too hot" can now actually be honored, wired through `INTENT_SCHEMA`/`has_enough_signal`/the no-matches summary. (3) AI-provider fallback replies (`FALLBACK_REPLY`) were being permanently saved into `SavedConversation` history like real answers - now skipped, same as `save=false`. (4) The save-status indicator had no awareness of a conversation's own `is_full` flag - now reflects it (`currentConversationIsFull`). (5) A saved conversation's sidebar entry went stale while it stayed active - now refreshes on every continued save, not just brand-new ones. (6) `trip_type`/`cost_of_living` choices were duplicated across `travel.models`/`users.models`/`ai.orchestration` by hand - `travel.models` is now the single canonical source (zero migrations, confirmed via `makemigrations --check`). (7) The 5-conversation cap had a TOCTOU race under concurrent tabs - closed with `select_for_update()` on the user row inside `transaction.atomic()`. 4 test changes (3 new, 1 rewritten), 204/204 total passing, `ruff check .` clean. Verified live against the real OpenAI API: Monaco-style feedback now gets a real reply with zero DB rows created, "not too hot" correctly returns only genuinely mild destinations, and the size-limit indicator fires and persists correctly. Full account in `DEVELOPMENT_LOG.md`.

- [x] **Flight provider interface scaffolded ahead of KAYAK access** ✅ 2026-09-02. Direct request: get the internal flight-provider interface ready now, so wiring in a real KAYAK adapter later is all that's left, even though KAYAK access itself is still gated (per `DECISIONS_PENDING.md` §4's research - needs ~1M+ monthly visitors / manual business approval Wanderes doesn't have). New `integrations/flights/` package (mirrors `integrations/climate/`'s ABC + normalized dataclass + settings-factory pattern exactly, per the shape `10_EXTERNAL_INTEGRATIONS.md` §13.1/§13.2 already designed): `FlightProvider`/`FlightOption` in `base.py`, `get_flight_provider()` factory reading a new `FLIGHT_PROVIDER` setting (defaults unset), and `kayak.py` registered as the `"kayak"` provider but **honestly a skeleton** - all three interface methods raise `NotImplementedError`, since there's no real KAYAK API documentation to implement correctly against pre-approval. Not wired into orchestration/scoring/UI at all - nothing real exists yet to surface. `HotelProvider`/`HotelOption` deliberately not scaffolded (request was flights-only; hotels stay fully undecided). 6 new tests, 210/210 total passing, `ruff check .`/`manage.py check` clean, zero new migrations. Verified via Django shell that the factory and the skeleton both fail in exactly the intended way. Full account in `DEVELOPMENT_LOG.md`.

- [x] **AI now confirms TravelerProfile details before ever suggesting a destination** ✅ 2026-09-02. Direct request: "check if IA can extract informations from userprofile, IA must always confirm this information with the user before suggest any destination." The extraction half was already true (see the entries below); this adds the missing confirm-first step.
  - Gated deterministically in Python, not left to AI judgment - this module's history has repeatedly shown LLM-decided control flow (temperature inference, clarification loops) causes real bugs. A new Redis flag (`ai.memory.is_profile_confirmed()`/`mark_profile_confirmed()`, same backend/TTL as turn-history memory, separate key, works identically for saved and unsaved conversations) tracks whether a conversation has already been through the one-time confirmation ask.
  - New gate in `stream_travel_recommendation`, right before `generate_recommendations` ever runs: if the traveler has confirmable profile data (home_country/travelers_count/complete budget triple) and this conversation hasn't confirmed it yet, a new `_build_profile_confirmation_messages()` prompt asks for confirmation with zero destinations suggested. The flag is marked immediately (not conditional on detecting an explicit "yes") - the very next message proceeds straight to real suggestions, matching the module's established low-friction "never re-ask" philosophy.
  - **Known scope boundary**: doesn't extend to `_build_open_ended_messages`'s "explicitly invited to guess" sub-case (rare, and not structurally separable in Python without a bigger restructure) - noted explicitly, not silently skipped.
  - 3 new tests + 2 existing tests updated to prime the bypass, 234/234 total passing, `ruff check .` clean, no new migrations. **Verified live against the real OpenAI API** - a real two-turn conversation showed the exact intended behavior: turn one confirmed home country/travelers/budget with zero destinations, turn two (next message) returned real ranked destinations immediately.
- [x] **Budget currency: collected explicitly, always converted to USD before reaching the AI** ✅ 2026-09-02. Direct follow-up: "budget must be always on dolar... user must select his currency and store the currency also... the agent must also check for this currency in dolar and convert to estimate."
  - New `users/currency.py` - `USD_CONVERSION_RATES` (24 currencies, static/approximate, documented as needing periodic manual updates), `CURRENCY_CHOICES`, `convert_to_usd()` (never raises, `None` on unknown currency). A deliberate static table, not a new external exchange-rate provider - matches the user's own "convert to estimate" framing and the project's existing approximate-data pattern.
  - `TravelerProfile.budget_currency` added (migration `users.0003_travelerprofile_budget_currency`) - required alongside `budget_amount`/`budget_period` (form `clean()` now requires all three together or none).
  - `ai.orchestration`'s traveler-context note now always converts `budget_amount` to an approximate USD figure before it ever reaches the AI - never the raw currency-ambiguous number.
  - `/users/profile/`'s budget row gained a third field (new `.field-row-3` CSS, same mobile-collapse pattern as `.field-row`).
  - 6 new/updated tests, 231/231 total passing, `ruff check .` clean. Verified live: currency `<select>` renders all 24 options correctly, full budget row (amount+currency+period) saves and persists correctly, confirmed via direct database check.
- [x] **TravelerProfile's new fields (home_country/travelers_count/budget) wired into the AI chat pipeline** ✅ 2026-09-02. Direct follow-up ("sim, pode fazer isso agora") to the gap flagged in the entry below - the fields were being collected but never used.
  - **Not wired into `recommendations.scoring`'s hard constraints** - `budget_amount` has no stored currency, so comparing it numerically against `Destination.cost_of_living`'s abstract 1-5 tier would require an unstated currency assumption. Instead, all three fields flow into `ai.orchestration`'s prompt-building layer as context the AI can use or ignore per reply - the same "recommendation philosophy" (Phase 11) already applied to every qualitative signal the deterministic scorer doesn't cover.
  - New `_traveler_profile(user)` / `_traveler_context_note(profile)` helpers append a "use only if relevant" note (home country, travelers count, budget+period - only when *both* budget fields are actually set) to the three AI-reasoning prompt builders (`_build_explanation_messages`, `_build_no_matches_messages`, `_build_open_ended_messages`).
  - New `_entry_requirements_note(profile, destinations)` - for real ranked candidates only (not AI-suggested no-match destinations), looks up `travel.services.get_entry_requirements()` per destination country and tells the AI whether the traveler's own nationality needs a visa - a **deterministic DB lookup**, not AI general knowledge, always paired with `ENTRY_REQUIREMENT_DISCLAIMER`. This is the connection point left open at the end of this morning's `CountryEntryRequirement` work.
  - 5 new tests, 229/229 total passing, `ruff check .` clean, no new migrations. Verified live against the real OpenAI API with a real full profile and the full 384-destination catalog - correct, quiet behavior confirmed (no fabricated mention for destinations without real entry-requirement data on file).
- [x] **Account/profile pages redesigned; `TravelerProfile` gained budget, travelers count, and home country** ✅ 2026-09-02. Direct request: make `/users/account/` and `/users/profile/` match the site's visual identity (both had been flagged bare in two prior UX passes), and collect budget (per day/week/month), number of travelers, and country of origin.
  - `TravelerProfile` gained `home_country` (free-text CharField, matching `Destination.country`'s convention), `travelers_count`, `budget_amount` + `budget_period` (new `BUDGET_PERIOD_CHOICES` - day/week/month) - all optional. New migration `users.0002_travelerprofile_budget_amount_and_more`. **Not wired into `recommendations.scoring`/`ai.orchestration` in this pass** - collecting the data was the request; converting it into a scoring constraint is a separate follow-up decision, noted in the model's docstring.
  - `TravelerProfileForm` extended with explicit labels and a `clean()` cross-field check (budget amount and period must be given together or not at all). "Profile completed" analytics event now also fires from any of the three new fields, not just the original two.
  - `/users/account/` redesigned as a header card (avatar initials, email, join date) + a grid of icon quick-link cards; `/users/profile/` redesigned into two labeled sections in one card, with budget amount/period laid out side by side. Both now use `{% trans %}`/`{% blocktrans %}` throughout (previously had none). New CSS added to `static/css/main.css`, built entirely from existing design tokens.
  - 4 new tests, 224/224 total passing, `ruff check .` clean. Verified live in a real browser: registered a test user, saved the full profile form, confirmed every value round-tripped correctly on reload and matched the database directly.
- [x] **Destination catalog expanded from 18 to 384 entries** ✅ 2026-09-02. Resolves "item 2" from the pending-items list, queued earlier the same day and interrupted by the country-entry-requirements work below. The user had already chosen the approach via `AskUserQuestion`: generate directly from Claude Code's general knowledge (~400 entries), accepting the same reliability caveat already applied to the original 18 and to `country_entry_requirements.json`.
  - Added 366 new entries to `travel/data/curated_destinations.json` (384 total), same schema and Portuguese-language convention as the original 18. Built in ten sequential batches by world region for broad, deliberate coverage: Western/Northern Europe; Nordics + Eastern Europe/Balkans/Greece/Russia; East + Southeast Asia; South Asia + Middle East; Africa; North America + Central America + Caribbean; South America; Oceania + Pacific Islands + Central Asia/Caucasus; a cross-region fill-in batch; a final rounding-out batch.
  - **Deliberately mixed `trip_type` and `cost_of_living` throughout rather than clustering by region** - both feed `recommendations.scoring`'s hard constraints/ranking directly, so a lopsided catalog would have silently degraded recommendation quality for underrepresented combinations. Final loaded distribution: culture 117, nature 107, beach 84, city 76; cost tiers 1→86, 2→97, 3→103, 4→69, 5→29.
  - Verified end-to-end in Docker: a script confirmed 384 total entries, zero duplicate slugs, no missing required fields before loading; `load_destinations` reported "Loaded 366 new, updated 18 existing" (exactly the expected split); 220/220 tests passing (destination count isn't asserted anywhere, so this is a real regression check); `ruff check .` clean; spot-checked several entries (Tóquio, Rio de Janeiro, Sydney, Cartagena, Samarcanda) via Django shell against the loaded DB rows.
  - Same accepted caveat as the rest of this dataset: general knowledge, not verified against a live travel-data API - approximate coordinates, general seasonal guidance, no live pricing. Full account in `DEVELOPMENT_LOG.md`.
- [x] **New `travel.CountryEntryRequirement` table (visa/vaccine/insurance guidance)** ✅ 2026-09-02. Direct request, interrupting the queued "item 2" (destination catalog expansion) work: research visa/vaccine/insurance entry requirements per country. Flagged first (before implementing) that this is a genuinely higher-stakes risk category than destination facts - wrong visa/vaccine info can mean denied boarding/entry or a real legal/health problem, and visa requirements depend on the *traveler's own nationality*, not just the destination. The user's own example ("EUA: precisam de visto: Brasil, Argentina, Uruguai...") confirmed the desired nationality-aware shape and, implicitly, generating from general knowledge (same accepted-caveat approach already chosen for the destination catalog).
  - New model **`travel.CountryEntryRequirement`** - `country` (unique), `visa_required_nationalities` (JSONField list - deliberately a list, not a flag, to keep the nationality dependency structural), `visa_notes` (free text for waiver-program nuance), `vaccine_requirements` (JSONField list), `insurance_required` (bool) + `insurance_notes`, `other_requirements` (JSONField list). The model's own docstring permanently carries the safety framing (mandatory disclaimer, non-exhaustive coverage), not just this log entry.
  - New **`travel.services.ENTRY_REQUIREMENT_DISCLAIMER`** (must accompany this data anywhere it's ever shown, enforced by convention like `05_AI_DESIGN.md` §7 already requires for AI content) and **`get_entry_requirements(country_name)`** (case-insensitive lookup, returns `None` rather than a false "no requirements" when nothing is on file).
  - **`travel/data/country_entry_requirements.json` + `load_country_requirements` management command** (same `update_or_create` pattern as `load_destinations`) - populated **40 countries** (the 18 existing curated destinations + ~22 other major destinations), explicitly **not** all ~195 UN member states - stated honestly in the file's own `$schema_note`. Leans on describing each country's visa-exemption *structure* (Schengen, US Visa Waiver Program, e-Visa/eTA systems) rather than attempting to enumerate every nationality. **Three entries (Brazil, Russia, China) are explicitly flagged low-confidence/volatile** in their own `visa_notes` rather than given the same confidence as stable entries (Schengen, Maldives).
  - Admin-registered for visibility/manual correction, matching `Destination`'s pattern.
  - **Deliberately not wired into the AI chat pipeline, orchestration, or any UI yet** - the request was for the table itself; surfacing it in chat replies (disclaimer phrasing, intent extraction, no-data handling) is a separate integration decision left for its own explicit ask.
  - 10 new tests (model creation + unique-country constraint, `get_entry_requirements`'s case-insensitivity/whitespace/no-match paths, load-command path-exists/creates/rerun-updates-not-duplicates), 220/220 total passing, `ruff check .` clean, one new migration (`travel.0002_countryentryrequirement`). Verified via Django shell against the real loaded data: 40 entries, spot-checked the US entry's `visa_required_nationalities` and Tanzania's Yellow Fever vaccine note both loaded correctly. Full account in `DEVELOPMENT_LOG.md`.
- [x] **Render/local/CI infrastructure renamed `travelagent-*` → `wanderes-*`** ✅ 2026-09-02, resolved better than the documented risk predicted. Resolves the one identifier group flagged-but-unresolved since the 2026-09-01 rebrand. Verified Render's actual documented behavior first (fetched their docs directly rather than guessing): Blueprint sync matches resources **by name**, so changing `name:` in `render.yaml` and re-syncing doesn't rename an existing resource in place - it provisions a **new** one, leaving the old one orphaned. Presented this to the user with a recommendation to leave it alone (purely cosmetic); the user chose to check the Render dashboard for a display-only rename option instead, then said losing the production data was fine either way. Code renamed everywhere (`render.yaml`, `docker-compose.yml`, `.env.example`, the real local `.env`, `config/settings/base.py`, `.github/workflows/ci.yml`, `README.md`) and validated locally end-to-end (`docker compose down -v` → rebuild → `migrate` 25/25 → `load_destinations` 18/18 → 210/210 tests → `ruff check .` clean → real `/health/` OK). **On the Render side, the user renamed all four resources directly via each service's Settings → Name field - and this turned out to be a genuine in-place rename, not the risky Blueprint-name-mismatch path**: confirmed live afterward that both `https://wanderes.com/health/` and the old `https://travelagent-web.onrender.com/health/` URL resolve to the same healthy service (`{"status": "ok", "database": "ok"}`), and the landing page still shows the real, previously-loaded curated destinations (Bali, Lisboa, Reykjavik) rather than an empty fresh database - i.e. no data was lost, no new orphaned resources were created. This means Render's dashboard "Name" field is a separate, safe, cosmetic rename distinct from the Blueprint YAML's `name:` matching key - not clearly documented publicly, worth remembering for next time. Since `render.yaml` already declared the matching `wanderes-*` names, everything is now aligned for future Blueprint syncs with no further action needed.

## What exists in the repo right now

```
Wanderes/
├── .github/workflows/ci.yml       CI: lint (ruff) + tests, against real Postgres+Redis
├── config/                        Django project (settings split by environment, urls, wsgi/asgi, celery.py)
├── core/                          Infra-only app: GET /health/ (checks DB connectivity) + test
├── documentation/                 Architecture docs (01–15) + this tracking set
├── users/                         Custom User (email login) + TravelerProfile
├── travel/                        Destination model + `load_destinations`; CountryEntryRequirement model (visa/vaccine/insurance, 2026-09-02) + `load_country_requirements`
│   └── data/curated_destinations.json, country_entry_requirements.json   Approved destination dataset (Phase 3, 18 entries; moved here from documentation/ 2026-08-30, see Phase 18 deploy entry) + entry-requirements dataset (40 countries, 2026-09-02)
├── trips/                         Trip (+ CRUD at /trips/), TripFlight, TripAccommodation, Feedback, TravelHistoryEntry (+ CRUD at /trips/history/); tasks.py/signals.py - learning from feedback (Celery)
├── integrations/                  climate/ - ClimateProvider interface + Open-Meteo adapter; flights/ - FlightProvider interface + KAYAK skeleton adapter (no working KAYAK access yet) (no models)
├── ai/                             provider/ (incl. streaming) + orchestration.py + prompts.py (Wander) + memory.py (Redis-backed short-term context) + conversations.py (saved-conversation limits/persistence) + models.py (SavedConversation, this app's first model); views.py/urls.py/templates - the /chat/ page + saved-conversations sidebar
├── recommendations/                scoring.py - deterministic recommendation scoring/ranking (no models)
├── analytics/                     Event model (self-hosted first-party analytics) + services.py/metrics.py, admin-registered
├── static/                        css/main.css + js/main.js - shared site-wide styling/nav toggle/dismissible messages
├── requirements/                  base.txt / development.txt / production.txt
├── docker-compose.yml             db (Postgres 16) + redis (7) + web + worker (celery) - pins `target: runtime`
├── Dockerfile                     multi-stage: runtime (local dev/test, unchanged) + production (Phase 18, gunicorn+whitenoise, final stage)
├── render.yaml                    Render Blueprint (Phase 18) - web + worker + Postgres + Key Value, deployed live at travelagent-web.onrender.com
├── .env.example                   template for local .env (never commit real .env)
├── manage.py, pyproject.toml (ruff+pytest config), .gitignore, .dockerignore, README.md
```

`users`, `travel`, and `trips` now exist (Phase 4, 2026-08-29). `recommendations`, `ai`, and `integrations` still don't — deliberately, until the phases that need them (AI orchestration, provider adapters) are reached. See `DEVELOPMENT_LOG.md`.

## Known environment gaps (not decisions — just missing local tooling)

- **Docker Desktop is installed and its engine works** (validated 2026-08-28 — see resolved section above). Virtualization is enabled; WSL2 backend starts cleanly.
- **A PostgreSQL 18 server is also installed and running as a Windows service** (`postgresql-x64-18`, listening on `0.0.0.0:5432`) — pre-existing system state, not created for Wanderes, unrelated to Wanderes's Dockerized Postgres 16. Claude Code does not have its credentials and has not touched it. Note: Docker Compose's `db` service also publishes host port 5432 and bound successfully during validation — the two do not appear to conflict in practice (likely because they're not both listening at the same time), but avoid running both simultaneously if port-binding errors ever appear.
- Local Python available via the `py` launcher (3.14). A `.venv` exists at the repo root with dependencies installed; `python manage.py check` and `ruff check .` both pass outside Docker too.
- The Docker image pins Python 3.12 (more predictable wheel availability for `psycopg`) — confirmed working (3.12.14 inside the built image).
- Local `.env` now exists (gitignored) with a generated `DJANGO_SECRET_KEY`, copied from `.env.example`. Future sessions on this machine don't need to recreate it — it also has a real `OPENAI_API_KEY` as of Phase 9's live-testing session (2026-08-29).
- **GitHub remote configured** (2026-08-29): `origin` → `https://github.com/wanderes-dev/wanderes.git`, branch `master` pushed and tracking `origin/master`. Push authentication goes through Windows Git Credential Manager — the cached credential is for the `wanderes-dev` GitHub account (a different, earlier-cached `cantarino10` credential was rejected since it didn't have access to this repo). If push auth ever fails again with "Repository not found," check which GitHub account GCM is using (`git credential fill` with `protocol=https`/`host=github.com`) before assuming the repo itself is missing.

## Next actionable steps

Per `15_IMPLEMENTATION_GUIDE.md`'s phase list, Phases 0-17 are done. **Phase 18 (MVP Validation) is live and actively in progress** - the deploy itself is done (see the summary line at the top of this file), and the app has been in continuous real use since. Phase 18 has no formal "done" checkbox; per the guide, Claude Code's role in it is exactly what's been happening since deploy - fix bugs, implement validated improvements, analyze usage data, improve tests - while the Human's role is talking to users, watching behavior, and deciding what's worth building next. **Confirmed explicitly with the user (2026-09-01)**: continue in this same mode - live-testing-driven bug fixes and UX improvements - rather than jumping ahead to Phase 19 (Premium Strategy), which requires human decisions (pricing, free/premium limits, billing) not yet made. Don't start building monetization/premium features speculatively; wait for those decisions or for the user to explicitly ask to move to Phase 19.

**"Item 2" (destination catalog expansion) is now done** - see the checklist entry below. No other queued item remains from the "pendências" list beyond what's already tracked in `DECISIONS_PENDING.md` (flight/hotel provider, Premium/pricing).

## Resume checklist for a fresh Claude session

If you're picking this up cold:

1. Read this file first.
2. Read `DECISIONS_PENDING.md` to see if the human has since made a decision (if so, update this file and proceed to the corresponding phase).
3. Read `DEVELOPMENT_LOG.md` for full historical context on what was built and why.
4. Do **not** re-implement Phase 0/1 — it's done and fully validated in Docker. Do **not** jump ahead to Phase 4+ without confirming Phase 2/3 decisions are actually resolved.
5. If Phase 2/3 are still unresolved, the only useful next step is asking the user for those decisions (see `DECISIONS_PENDING.md`) — do not guess AI/travel-data providers.
